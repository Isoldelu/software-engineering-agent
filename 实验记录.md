# 实验记录

本文件用于按顺序记录 AI Software Engineering Agent 的每一步实验变化。后续每次新增功能只在末尾追加，不覆盖已有记录。

## Step 1：项目骨架与模拟数据集

目标：搭建一个合规的 AI4SE Agent Demo 最小项目骨架。

新增内容：

- 创建 `Software-Agent/` 项目目录。
- 创建 `data/packages.json`，模拟软件包、版本、架构、release、文件列表。
- 创建 `data/dependencies.json`，模拟软件包依赖关系。
- 创建 `data/versions.json`，模拟版本升级变化。
- 创建 `data/documents/release_note.md` 和 `data/documents/software_manual.md`，为后续 RAG 做准备。
- 创建 `tools/`、`agent/`、`evaluation/` 基础目录。
- 创建初版 `README.md`。

实现结果：

- 项目具备软件资产知识环境。
- 数据全部为模拟数据，不包含任何内部数据。

## Step 2：Agent Tools

目标：把确定性软件分析能力封装为可调用工具。

新增/修改内容：

- 实现 `PackageSearchTool`，支持按包名或 release 查询软件包信息。
- 实现 `DependencyAnalysisTool`，支持查询软件包直接依赖。
- 实现 `VersionCompareTool`，支持查询版本变化。
- 更新 `workflow.py`，支持根据 Router 调用工具。
- 修复 `main.py`，让入口可以运行工具调用流程。

实现结果：

- 支持软件包查询、依赖分析、版本比较三类基础任务。
- 工具输出包含结构化结果和 evidence 路径。

## Step 3：最小 Agent Workflow

目标：跑通 `query -> router -> tool -> observation -> answer -> trajectory`。

新增/修改内容：

- 更新 `router.py`，支持意图识别、工具选择和简单参数抽取。
- 更新 `workflow.py`，新增 `run_agent()`。
- Agent 输出包含 `intent`、`selected_tool`、`arguments`、`answer`、`used_tools`、`evidence`、`confidence`、`trajectory`。
- 新增 `evaluation/eval_runner.py`，支持初步自动评测。

实现结果：

- 项目从单纯工具调用升级为最小 Agent。
- 初版评测输出工具选择准确率和答案准确率。

## Step 4：组件归属、轨迹持久化与评测增强

目标：补齐软件资产分析中常见的“文件属于哪个包”问题，并强化 Agent 轨迹和评测。

新增/修改内容：

- 新增 `ComponentMappingTool`，支持 `libssl.so 属于哪个软件包` 这类组件归属查询。
- 新增 `agent/memory.py`，将 Agent 执行轨迹追加写入 `data/trajectories.jsonl`。
- 扩展 `router.py`，支持 `component_mapping` 意图。
- 扩展 `workflow.py`，支持组件反查和轨迹持久化。
- 扩展 `evaluation/test_cases.json`，新增组件反查用例。
- 增强 `eval_runner.py`，输出：
  - Tool Routing Accuracy
  - Task Success Rate
  - Answer Grounding Accuracy
  - Answer Accuracy
  - Average Tool Calls
  - Bad Case List

实现结果：

- 项目能够支撑“软件资产知识构建 + Agent 轨迹记录 + Agent Evaluation”的简历叙述。
- 评测集扩展到 8 条。

## Step 5：RAG 知识增强

目标：让 Agent 能处理 release note 和 software manual 等非结构化研发文档。

新增/修改内容：

- 新增 `rag/embedding.py`，实现轻量 token 表示。
- 新增 `rag/vector_store.py`，实现内存版 token-overlap 检索。
- 新增 `rag/retriever.py`，实现 Markdown 文档加载、按标题切块和检索。
- 新增 `tools/rag_tool.py`，封装 `RAGRetrieverTool`。
- 更新 `router.py`，支持文档检索类问题。
- 更新 `workflow.py`，支持 RAG 工具回答生成。
- 扩展 `evaluation/test_cases.json`，新增 release note 和 software manual 检索用例。
- 优化 RAG 排序：去掉停用词、标题命中加权、manual/release note 查询加源文件偏好。

实现结果：

- 项目从结构化 JSON 查询扩展为“结构化数据 + 非结构化文档检索”。
- 评测集扩展到 10 条。

## Step 6：多工具规划 / Hybrid Agent

目标：支持复合任务中的多工具编排。

新增/修改内容：

- 新增 `agent/planner.py`，将用户问题转换为单工具或多工具执行计划。
- 更新 `workflow.py`，支持执行多工具计划。
- 支持后续工具消费前序工具结果，例如：
  - 先用 `RAGRetrieverTool` 检索 release note。
  - 再用 `PackageSearchTool` 查询 release 包列表。
  - 最后对每个包调用 `DependencyAnalysisTool`。
- 更新 `eval_runner.py`，支持 `expected_tools` 工具序列评测。
- 扩展 `test_cases.json`，新增 Hybrid Agent 用例。

实现结果：

- 支持 `1214 release packages and their dependencies` 这类复合问题。
- 实际工具链路为 `rag_retrieval -> package_search -> dependency_analysis`。
- 评测集扩展到 12 条，平均工具调用次数从 1.0 提升到 1.4167，更能体现多工具 Agent 特征。

## Step 7：Prompt / Tool Schema 层

目标：让当前确定性 Planner 具备后续替换为 LLM Router、Function Calling 或 LangGraph 的接口基础。

新增/修改内容：

- 新增 `agent/prompt.py`。
- 定义 `SYSTEM_PROMPT`，强调工具验证和不猜测原则。
- 定义 `TOOL_SCHEMAS`，集中描述每个工具的功能、输入和输出。
- 定义 `PLANNER_OUTPUT_FORMAT`，规范未来 LLM Planner 应输出的 JSON 计划格式。
- 新增 `build_planner_prompt(query)`，可直接生成 LLM Router / Planner prompt。
- 新增 `export_tool_schemas()`，为后续 LangGraph 或 Function Calling 接入预留接口。
- 更新 `planner.py`，为每个计划附带 `planner_prompt` 快照。
- 更新 `workflow.py`，在结果和轨迹中记录 `tool_schema_version`。

实现结果：

- 当前项目仍保持确定性可测，但已经具备接入 LLM Planner 的提示词和工具 schema 基础。
- 面试时可以解释为：先用规则 Planner 保证稳定闭环，再将 prompt/schema 层抽象出来，为后续 LLM Router 替换做准备。

## Step 8：LLM Router / Function Calling 适配层

目标：在不依赖真实大模型 API 的前提下，打通“LLM 输出 JSON plan -> 本地校验 -> Agent 执行”的接口，为后续接入 LLM Router、OpenAI Function Calling 或 LangGraph 做准备。

新增/修改内容：

- 新增 `agent/llm_router.py`。
- 实现 `parse_llm_plan(query, llm_output)`，支持解析 LLM 风格 JSON 计划。
- 实现 `validate_plan(plan)`，校验 plan 是否包含合法 intent、tool、steps 和 arguments。
- 实现 `build_function_specs()`，将本地 `TOOL_SCHEMAS` 转换为 function-calling 友好的函数规格。
- 新增 `examples/llm_plan_hybrid.json`，模拟真实 LLM 产出的多工具计划。
- 更新 `planner.py`，支持接收外部 LLM plan；如果未传入外部 plan，则继续使用确定性规则 planner。
- 更新 `workflow.py`，支持 `llm_plan_output` 参数，并记录 `planner_source`。
- 更新 `main.py`，支持命令：
  - `python main.py --llm-plan examples/llm_plan_hybrid.json "1214 release packages and their dependencies"`
- 更新 `README.md`，补充 LLM Router 适配层说明。

实现结果：

- 项目现在既可以使用确定性 Planner，也可以消费外部 LLM JSON plan。
- Agent 执行器与 Planner 解耦，后续替换成真实 LLM Router 时，不需要重写工具执行层。
- 面试时可以解释为：当前 demo 用本地规则保证稳定评测，同时预留了 LLM Router / Function Calling 接口，体现工程可扩展性。

## Step 9：工程化整理

目标：将项目从“实验代码”整理为更接近开源项目的工程结构，便于 GitHub 展示、面试讲解和后续扩展。

新增/修改内容：

- 新增 `app/` 顶层应用包。
- 将原有 `agent/`、`tools/`、`rag/` 迁移到：
  - `app/agent/`
  - `app/tools/`
  - `app/rag/`
- 修正所有 Python import 路径，例如：
  - `from agent.workflow import run_agent` 改为 `from app.agent.workflow import run_agent`
  - `from tools.package_tool import PackageSearchTool` 改为 `from app.tools.package_tool import PackageSearchTool`
- 修正 `PROJECT_ROOT` 路径计算，确保移动到 `app/` 后仍然能读取 `data/` 下的模拟数据。
- 新增 `requirements.txt`，列出当前和后续阶段需要的依赖：
  - `numpy`
  - `pydantic`
  - `pytest`
  - `openai`
  - `langchain`
  - `langgraph`
  - `faiss-cpu`
  - `sentence-transformers`
  - `fastapi`
  - `uvicorn`
- 新增 `.gitignore`，忽略 `__pycache__`、虚拟环境、环境变量文件和运行时轨迹文件。
- 新增 `tests/`：
  - `tests/test_agent_workflow.py`
  - `tests/test_tools.py`
- 重写 `README.md`，补充：
  - 项目简介
  - Architecture
  - Features
  - Project Structure
  - Demo 命令
  - Evaluation 指标
  - Development 和 Roadmap

实现结果：

- 项目结构从脚本式目录升级为 `app/` 包结构。
- GitHub 首页内容更加完整，能直接展示项目定位、架构、Demo 和评测结果。
- 后续接 FastAPI、FAISS、LangGraph 或真实 LLM Router 时，目录边界更清晰。

## Step 10：FastAPI 服务化

目标：把当前命令行 Agent Demo 封装成可调用的 HTTP 服务，便于面试展示、系统集成和后续前端/可视化接入。

新增/修改内容：

- 新增 `app/api/` 服务层目录。
- 新增 `app/api/schemas.py`，定义 API 请求/响应数据结构：
  - `AgentQueryRequest`
  - `AgentQueryWithPlanRequest`
  - `AgentQueryResponse`
  - `EvaluationResponse`
  - `ToolListResponse`
- 新增 `app/api/server.py`，实现 FastAPI 应用。
- 新增接口：
  - `GET /health`
  - `POST /agent/query`
  - `POST /agent/query-with-plan`
  - `GET /evaluation/run`
  - `GET /tools`
  - `GET /function-specs`
- 重构 `evaluation/eval_runner.py`，抽出 `run_evaluation()` 函数，让 CLI 和 API 可以复用同一套评测逻辑。
- 更新 `README.md`，补充 FastAPI 启动命令和接口说明。

实现结果：

- 当前项目从命令行 Demo 升级为可服务化调用的 Agent API。
- 面试时可以展示：自然语言 query 通过 `/agent/query` 进入 Agent，返回 plan、used_tools、answer、evidence 和 trajectory。
- `/agent/query-with-plan` 可以演示外部 LLM JSON plan 进入统一执行器的流程。

## Step 11：浏览器 Demo 页面

目标：在 FastAPI 服务基础上增加一个轻量可视化页面，让面试展示时可以直接在浏览器中输入 query，并观察 Agent 的 answer、used tools、plan、evidence 和 trajectory。

新增/修改内容：

- 新增 `app/api/demo.py`。
- 在 `demo.py` 中实现内嵌 HTML/CSS/JavaScript 页面。
- 页面支持：
  - 输入自然语言 query。
  - 一键运行 Agent。
  - 展示 intent、tool call count、success。
  - 展示最终 answer。
  - 展示 used_tools、plan、evidence、trajectory。
  - 提供常用示例 query。
- 更新 `app/api/server.py`，新增：
  - `GET /demo`
- 更新 `README.md`，补充浏览器 Demo 入口。

实现结果：

- 启动 FastAPI 后，可以访问 `http://127.0.0.1:8000/demo` 进行可视化演示。
- 项目展示方式从命令行/API 文档进一步升级为可交互浏览器 Demo。
- 面试时可以直接演示 Hybrid Agent 的多工具规划过程，而不只展示 JSON 输出。
## Step 12：Bad Case 优化闭环

目标：建立一套可复用的 Agent Bad Case 优化流程，让项目不只展示“能回答”，还展示“能评测、能归因、能迭代优化”。

新增/修改内容：
- 新增 `evaluation/challenge_cases.json`，作为困难样本集，和主 benchmark 分离。
- 新增 `evaluation/bad_case_report.md`，记录本轮坏例发现、原因归类、代码修复和剩余问题。
- 增强 `evaluation/eval_runner.py`：
  - 支持 `--suite benchmark`、`--suite challenge`、`--suite all`。
  - 新增 `run_bad_case_analysis()`。
  - 新增 `bad_case_count`、`bad_case_types`、`optimization_suggestions`。
  - 修复 bad case 判断逻辑，将 `answer_missing_expected_content` 也纳入坏例列表。
- 优化 `app/agent/planner.py`：
  - 新增 `hybrid_release_version_compare` 规划逻辑。
  - 支持 `release + version changes` 类型问题规划为多工具链路。
- 优化 `app/agent/workflow.py`：
  - 支持 `version_compare` 消费 `package_search` 的前序结果。
  - Hybrid answer 新增版本变更摘要。
- 更新 `app/api/server.py`：
  - 新增 `GET /evaluation/bad-cases`。
- 更新 `README.md`：
  - 补充 challenge suite 运行命令。
  - 补充 bad-case loop 说明。

实现结果：
- 主评测集保持稳定：
  - total: 12
  - tool_routing_accuracy: 1.0
  - task_success_rate: 1.0
  - answer_grounding_accuracy: 1.0
  - answer_accuracy: 1.0
  - bad_cases: []
- Challenge 集用于持续优化：
  - total: 5
  - 修复前存在 `wrong_tool` 类型坏例。
  - 修复后 `tool_routing_accuracy` 提升到 1.0。
  - 剩余坏例统一归因为 `tool_execution_failed`，主要原因是模拟知识库缺少 release 1215、legacycrypto.so、ethtool/tcpdump 版本变更记录。

面试表达：
> 我没有只做静态 Demo，而是把 Agent 执行过程沉淀为可评测闭环：先构造 challenge cases 暴露 Bad Case，再根据 wrong_tool、tool_execution_failed、answer_not_grounded 等原因做归因，最后针对高频问题优化 Planner、工具执行或知识库覆盖。

## Step 13：缺失数据感知与 Challenge 闭环收敛

目标：继续 Step 12 的 Bad Case 优化，将剩余的 `tool_execution_failed` 分成两类处理：
- 应该补知识库的，补充模拟数据。
- 本来就查不到的，让评测支持 `not_found` 作为合理结果。

新增/修改内容：
- 更新 `data/versions.json`：
  - 新增 `ethtool` 版本变更记录。
  - 新增 `tcpdump` 版本变更记录。
- 更新 `app/tools/package_tool.py`：
  - 对不存在的 release 返回更明确的 `No package records were found for release ...`。
- 更新 `app/agent/workflow.py`：
  - Hybrid answer 新增 `Missing records` 摘要。
  - 当多工具链路中部分工具查不到数据时，最终回答会显式说明缺失记录。
- 重写并增强 `evaluation/eval_runner.py`：
  - 新增 `expected_status` 评测字段。
  - 支持 `success`、`not_found`、`partial_success` 三类预期状态。
  - 将“合理查不到”的样本从坏例中剥离出来，避免误判。
- 更新 `evaluation/challenge_cases.json`：
  - 将 `1215 release packages and their dependencies` 标记为预期 `not_found`。
  - 将 `which package owns legacycrypto.so` 标记为预期 `not_found`。
  - 用新增版本数据验证 `ethtool` 和 `tcpdump` 版本变更能力。
- 更新 `evaluation/bad_case_report.md` 和 `README.md`，记录本轮闭环结果。

实现结果：
- 主 benchmark 保持全通过：
  - total: 12
  - tool_routing_accuracy: 1.0
  - task_success_rate: 1.0
  - answer_grounding_accuracy: 1.0
  - answer_accuracy: 1.0
  - bad_cases: []
- Challenge suite 从 Step 12 的剩余坏例收敛为全通过：
  - total: 5
  - tool_routing_accuracy: 1.0
  - task_success_rate: 1.0
  - answer_grounding_accuracy: 1.0
  - answer_accuracy: 1.0
  - bad_cases: []

代表性优化：
```text
输入：
1214 release packages and their version changes

工具链路：
rag_retrieval -> package_search -> version_compare -> version_compare

结果：
nginx 和 tcpdump 的版本变更都能被汇总回答。
```

面试表达：
> 我进一步把 Bad Case 分成“知识库覆盖不足”和“合理无记录”两类：前者通过补充模拟版本数据解决，后者通过 expected_status 机制纳入评测体系。这样 Agent 不只是追求回答成功，也能在查不到数据时给出可追溯、可评测的缺失说明。

## Step 14：更难的 Challenge Set / 模糊查询鲁棒性评测

当前问题：
- 前面的 benchmark 和 challenge 主要验证标准表达，例如 `openssl dependencies`、`1214 release packages and their dependencies`。
- 这类问题能证明 Agent 主链路正确，但还不能证明它能处理真实研发人员的模糊表达。
- 真实场景里用户更可能说：
  - `安全通信库依赖什么？`
  - `1214 里面抓包工具升级了啥？`
  - `libpcap.so 是谁依赖引入的？`
  - `查一下 nginx 的版本变化和依赖`

为什么要添加：
- 防止项目被认为只是固定关键词匹配或模板问答。
- 验证 Router/Planner 是否能处理领域别名、模糊表达、多意图问题和缺失数据问题。
- 让项目更接近 AI4SE Agent 的真实评测方式：不只看标准问题准确率，也看非标准表达下的鲁棒性。

新增/修改内容：
- 新增 `evaluation/robustness_cases.json`：
  - 覆盖领域别名：`安全通信库 -> openssl`。
  - 覆盖工具别名：`抓包工具 -> tcpdump`。
  - 覆盖功能别名：`网口诊断工具 -> ethtool`。
  - 覆盖反向依赖：`libpcap.so 是谁依赖引入的`。
  - 覆盖多意图规划：`查一下 nginx 的版本变化和依赖`。
  - 覆盖缺失 release：`1216 发布里新增了什么`。
- 更新 `app/agent/router.py`：
  - 新增 package alias mapping。
  - 支持组件反向依赖问题优先路由到 `dependency_analysis`。
- 更新 `app/tools/dependency_tool.py`：
  - 新增 reverse dependency 查询能力。
  - 支持回答 `libpcap.so is required by: tcpdump`。
- 更新 `app/agent/planner.py`：
  - 新增 `hybrid_package_dependency_version_analysis`。
  - 支持单个包同时查询依赖和版本变化。
- 更新 `app/agent/workflow.py`：
  - 将 Router/Planner 解析出的标准 package 参数传给工具执行层。
  - 支持 Hybrid answer 中展示反向依赖结果。
- 更新 `app/tools/rag_tool.py`：
  - 对 release query 做精确 release 过滤，减少 `1216` 误召回 `1213/1214` 的假阳性。
- 更新 `evaluation/eval_runner.py`：
  - 新增 `--suite robustness`。
  - `--suite all` 同时运行 benchmark、challenge、robustness。
- 更新 `app/api/server.py`：
  - 新增 `GET /evaluation/robustness`。
- 新增 `evaluation/robustness_report.md`。
- 更新 `README.md`，补充 robustness suite 运行方式和结果。

添加后的影响：
- Agent 不再只依赖标准包名，也能识别领域别名：
  - `安全通信库` 可以映射到 `openssl`。
  - `抓包工具` 可以映射到 `tcpdump`。
  - `网口诊断工具` 可以映射到 `ethtool`。
- Agent 支持新的问题类型：
  - 反向依赖分析。
  - 单包多意图规划。
  - 缺失 release 的稳健 not_found 回答。
- 项目评测体系从两层升级为三层：
  - `Software-Agent-Bench`：标准能力回归。
  - `Software-Agent-Challenge`：Bad Case 闭环。
  - `Software-Agent-Robustness`：模糊查询鲁棒性。

验证结果：
- 全量评测均通过：
  - `Software-Agent-Bench`: all metrics 1.0
  - `Software-Agent-Challenge`: all metrics 1.0
  - `Software-Agent-Robustness`: all metrics 1.0
- Robustness suite：
  - total: 6
  - tool_routing_accuracy: 1.0
  - task_success_rate: 1.0
  - answer_grounding_accuracy: 1.0
  - answer_accuracy: 1.0
  - bad_cases: []

代表性链路：
```text
输入：
查一下 nginx 的版本变化和依赖

工具链路：
package_search -> dependency_analysis -> version_compare
```

面试表达：
> 我在标准 benchmark 之外增加了鲁棒性评测集，专门覆盖真实研发人员可能使用的别名表达、模糊问题、多意图问题和缺失数据问题。这个过程暴露了 Router 识别结果没有传递到工具执行层、RAG 对 release 存在假阳性召回等问题，并通过 package alias、反向依赖查询、精确 release 过滤和多意图 Planner 进行了优化。

## Step 15：评测报告汇总 / 面试展示页增强

当前问题：
- 项目已经有三层评测：
  - `Software-Agent-Bench`
  - `Software-Agent-Challenge`
  - `Software-Agent-Robustness`
- 但是展示时仍然需要分别运行命令或查看较长 JSON。
- 面试时如果只展示 `/demo`，面试官能看到 Agent 执行过程，但不一定能快速看到系统整体稳定性。

为什么要添加：
- 将“能力演示”和“评测证明”分开展示。
- `/demo` 展示单次 query 的 Agent 执行链路。
- `/evaluation-dashboard` 展示三套评测的整体指标。
- 这样能更清楚地证明项目不是一次性 Demo，而是有持续评测体系的工程项目。

新增/修改内容：
- 更新 `evaluation/eval_runner.py`：
  - 新增 `run_all_evaluations()`。
  - 新增 `run_evaluation_summary()`。
  - 将 benchmark、challenge、robustness 三套评测汇总成统一 summary。
- 新增 `app/api/evaluation_dashboard.py`：
  - 实现浏览器评测 Dashboard。
  - 展示 suite 数量、总用例数、bad case 数、是否全部通过。
  - 展示每个 suite 的 routing、success、grounding、answer accuracy、平均工具调用次数。
- 更新 `app/api/server.py`：
  - 新增 `GET /evaluation/summary`。
  - 新增 `GET /evaluation-dashboard`。
- 更新 `app/api/demo.py`：
  - 新增鲁棒性示例 query。
  - 新增跳转到 Evaluation Dashboard 的入口。
- 新增 `evaluation/evaluation_summary_report.md`。
- 更新 `README.md`：
  - 补充 `/evaluation-dashboard`。
  - 补充 `/evaluation/summary`。
  - 补充最新 summary 指标。

添加后的影响：
- 面试展示路径更完整：
  - 先打开 `/demo` 展示 Agent 如何规划、调用工具、生成证据。
  - 再打开 `/evaluation-dashboard` 展示项目如何量化评测和验证稳定性。
- 评测结果从命令行 JSON 升级为浏览器可视化总览。
- 三层评测可以被统一说明：
  - 标准能力回归。
  - Bad Case 闭环。
  - 模糊查询鲁棒性。

验证结果：
```text
suite_count: 3
total_cases: 23
total_bad_cases: 0
all_suites_passed: true
```

新增入口：
```text
GET /evaluation/summary
GET /evaluation-dashboard
```

面试表达：
> 我把 Agent 的能力演示和评测证明分成两个页面：Demo 页面展示单次 query 的工具规划、证据和轨迹；Evaluation Dashboard 展示 benchmark、challenge 和 robustness 三套评测指标。这样可以证明项目不是只跑通一个样例，而是具备持续评测和回归验证能力。

## Step 15 补强：Agent Benchmark & Optimization Experiment

当前问题：
- 上一版 Step 15 已经完成了评测 Dashboard，但它主要是展示层。
- 按照新的目标，项目还缺少更有“算法味”的实验层：
  - 大规模 benchmark。
  - baseline 对比。
  - 优化前后对比。
- 如果只停留在 23 条评测，面试中仍可能被认为是小样例 Demo。

为什么要添加：
- 让项目从“工程 Demo”升级为“有实验结果的 Agent 项目”。
- 支撑简历中“通过 Bad Case 优化 Agent 策略”的说法。
- 让面试表达从“我做了 Agent”升级为“我构建了 benchmark、做了 baseline 对比，并通过优化提升了 tool routing accuracy”。

新增/修改内容：
- 新增 `evaluation/generate_large_benchmark.py`：
  - 自动生成大规模 benchmark。
- 新增 `evaluation/large_benchmark.json`：
  - 共 170 条样本。
  - 分类覆盖：
    - package_query: 30
    - dependency: 30
    - version_compare: 30
    - component_mapping: 20
    - rag_query: 30
    - hybrid_task: 30
- 更新 `evaluation/eval_runner.py`：
  - 新增 `--suite large`。
  - 新增 `--suite experiment`。
  - 将 large benchmark 加入 summary。
- 新增 `evaluation/experiment_runner.py`：
  - 实现离线 baseline 对比实验。
  - DirectLLMProxy：无工具，模拟直接回答。
  - RAGOnlyProxy：只做文档检索。
  - Agent：完整多工具 Agent。
  - LegacyKeywordRouter：模拟优化前关键词 Router。
- 新增 `evaluation/agent_optimization_experiment_report.json`：
  - 保存完整实验结果。
- 新增 `evaluation/agent_benchmark_experiment.md`：
  - 面试友好的实验报告。
- 更新 `app/api/server.py`：
  - 新增 `GET /evaluation/experiment`。
- 更新 `app/api/evaluation_dashboard.py`：
  - Dashboard 展示 baseline 对比和优化前后结果。
- 更新 `README.md`：
  - 补充 large benchmark、experiment 命令和结果。

实验中发现的问题：
- `what release contains nginx` 被 RAG 或 component mapping 抢路由。
- `secure communication library package info` 虽然 Router 识别了 alias，但执行层没有把标准 package 传给 PackageSearchTool。
- `1213 release packages and their dependencies` 中 `ethtool` 没有显式 dependency 记录，导致 hybrid plan 被判失败。
- `contains` 同时可能表示 release 包含 package，也可能表示 component 属于 package，存在规则冲突。

优化内容：
- `router.py`：
  - 增加 package metadata query 优先规则。
  - 增加 release contains query 优先规则。
  - 修复 `contains` 语义冲突。
- `workflow.py`：
  - PackageSearchTool 执行时优先使用 Planner 解析出的标准 package/release。
- `data/dependencies.json`：
  - 新增 `ethtool` 空依赖记录，显式表达“无依赖”。

实验结果：
```text
Large Benchmark:
total: 170
tool_routing_accuracy: 1.0
task_success_rate: 1.0
answer_grounding_accuracy: 1.0
answer_accuracy: 1.0
bad_cases: 0
```

Baseline 对比：
```text
DirectLLMProxy:
task_success_rate: 22.94%
answer_accuracy: 22.94%

RAGOnlyProxy:
task_success_rate: 57.06%
answer_accuracy: 57.06%

Agent:
task_success_rate: 100.00%
answer_accuracy: 100.00%
tool_accuracy: 100.00%
```

优化前后对比：
```text
Legacy keyword router tool accuracy: 61.76%
Optimized Agent tool accuracy: 100.00%
Absolute improvement: +38.24%
```

添加后的影响：
- 项目现在不仅有功能 Demo，还有可量化实验。
- 可以解释 Agent 相比 Direct LLM / RAG-only 的优势：
  - Direct LLM 缺少结构化工具，容易遗漏依赖、版本、组件归属细节。
  - RAG-only 适合文档问题，但不擅长结构化 package/dependency/version 查询。
  - Agent 能根据意图调用专业工具，并在 hybrid task 中组合多个工具。
- 可以解释优化过程：
  - 从 legacy keyword router 到 alias mapping + planner + tool argument normalization。
  - 通过 bad case 发现问题，再针对性修复。

面试表达：
> 我将评测从 23 条扩展到 170 条，覆盖 package query、dependency、version compare、component mapping、RAG query 和 hybrid task 六类任务。同时设计了 DirectLLMProxy、RAGOnlyProxy 和 Agent 三种方法对比。实验显示，Agent 在大规模 benchmark 上达到 100% task success，而 DirectLLMProxy 和 RAGOnlyProxy 分别为 22.94% 和 57.06%。此外，我用 LegacyKeywordRouter 模拟优化前策略，经过 alias mapping、planner 和参数标准化优化后，tool accuracy 从 61.76% 提升到 100%。

## Step 16：项目收尾与面试材料固化

当前问题：
- 项目功能、评测和实验已经较完整，但材料仍分散在 README、evaluation 报告和实验记录中。
- 如果直接用于 GitHub 或面试，面试官需要自己从多个文件里拼接项目定位、架构、实验结果和简历表达。
- README 原先更像开发过程说明，不够像一个开源项目首页。

为什么要添加：
- 将项目包装成可展示、可投递、可面试讲解的最终形态。
- 把技术实现、实验结果、面试话术、简历版本拆成清晰文档。
- 避免面试时临场组织语言，把关键结果和表述提前固化。

新增/修改内容：
- 新增 `docs/` 目录。
- 新增 `docs/project_summary.md`：
  - 项目定位。
  - 系统架构。
  - 核心设计。
  - 评测结果。
- 新增 `docs/experiment_results.md`：
  - 四套评测汇总表。
  - 170 条 large benchmark 分类。
  - baseline 对比。
  - 优化前后对比。
- 新增 `docs/interview_talking_points.md`：
  - 30 秒项目介绍。
  - 项目动机。
  - 技术亮点。
  - 常见面试问题回答。
  - 合规表述。
- 新增 `docs/resume_version.md`：
  - 简历项目标题。
  - 项目描述。
  - 职责描述。
  - 可直接放简历的 bullet points。
- 重写 `README.md`：
  - 将项目亮点和实验结果前置。
  - 增加 Mermaid 架构图。
  - 增加 Quick Start、API、Evaluation 和 Documentation 导航。
  - 增加合规说明。

添加后的影响：
- GitHub 首页可以直接展示项目价值：
  - Agent 能力。
  - 系统架构。
  - 实验结果。
  - baseline 对比。
  - bad-case 优化收益。
- 面试材料已经分层：
  - README 给面试官快速看。
  - project_summary 给项目总览。
  - experiment_results 给实验讨论。
  - interview_talking_points 给面试口述。
  - resume_version 给简历投递。
- 项目从“代码完成”进入“可展示完成”的状态。

最终项目定位：
> 一个面向软件研发场景的多工具 AI4SE Agent Demo，基于模拟软件资产数据实现 package 查询、依赖分析、版本比较、组件归属、RAG 检索、Hybrid Planning、Trajectory Logging、Benchmark Evaluation 和 Bad Case Optimization。

面试表达：
> 我最后将项目整理成开源展示形态：README 负责快速说明项目定位、架构和实验结果；docs 目录沉淀项目总结、实验结果、面试话术和简历版本。这样项目不仅有功能实现，也有可复现评测、可解释优化过程和清晰的面试表达材料。

## Step 17 规划：Roadmap V2 与受控自进化路线设计

当前问题：
- Step 1-16 已经完成五类工具、Hybrid Agent、轻量 RAG、trajectory、FastAPI、Demo 和 193 条评测，但后续建议同时包含可信执行、Hybrid RAG、多轮 Trace 和自进化，范围较大。
- 当前 `evidence` 主要是来源路径字符串，trajectory 缺少 trace/session/policy/latency，Bad-case 模块仍是离线建议，不能直接等同于完整 Evidence、Trace 或 Replay。
- 如果直接开始自动生成和发布策略，系统尚不能可靠判断候选策略是否真正改善，也无法定位或回滚一次变化。

为什么要添加：
- 将两份改造建议合并为单一依赖顺序，避免多个模块同时展开。
- 先冻结当前 193 条评测与 API 行为，再逐步增加 Evidence、Verifier、Hybrid RAG、Context/Trace 和受控优化闭环。
- 为后续每个 Step 统一“代码、测试、评测、文档、演示”验收标准。

新增内容：
- 新增 `docs/roadmap_v2.md`。
- 将下一阶段拆分为 Step 17-23：
  - Step 17：架构基线与兼容保护。
  - Step 18：Evidence 与 Citation。
  - Step 19：Verifier 与部分成功语义。
  - Step 20：Hybrid RAG。
  - Step 21：多轮 Context 与增强 Trace。
  - Step 22：Feedback 与受控 Bad-case Loop。
  - Step 23：策略版本化、灰度、回滚与工程交付。
- 明确自进化只作用于配置型策略资产，不允许自动修改源码、数据、测试断言、权限或发布门槛。

添加后的影响：
- 项目后续目标从“继续堆功能”调整为“先可信、再可观测、最后受控优化”。
- 每个阶段都有定量指标和独立演示，不会只完成目录或接口定义。
- 面试叙事可以逐步升级为 Evidence 可追溯、在线 Verifier、可量化 Hybrid RAG、可回放 Trace 和受控自进化闭环。

验证结果：
- 规划前重新执行当前评测汇总：4 个 suite、193 条 case、0 个 bad case，四项平均核心指标均为 1.0。
- 优化实验基线仍为：LegacyKeywordRouter tool accuracy 61.76%，Optimized Agent 100%，绝对提升 38.24%。
- 本步骤只新增规划与实验记录，没有修改运行时行为；下一实施步骤确定为 Step 17 基线与兼容保护。

## Step 17：架构基线与兼容保护

当前问题：
- 项目已有 193 条评测，但结果主要通过运行时汇总查看，没有保存包含逐条行为的机器可读兼容基线。
- 原有测试只有 4 条 Tool 测试和 2 条 Workflow 测试，没有覆盖 RAG Tool、not_found、API 响应字段和完整输出快照。
- 后续 Step 18 将改造 Evidence 和 Tool Observation，如果没有契约与 Golden Output，很难区分预期扩展和意外回归。
- 当前 Windows 中文路径和受限写入环境下，pytest 默认缓存插件会在测试结束阶段挂起。

为什么要添加：
- 冻结 Step 1-16 的现有行为，为后续架构重构建立可自动比较的参照物。
- 保护 `/agent/query`、五个 Tool 和 Hybrid Workflow 的公开语义。
- 让“193 条评测不下降”从人工查看指标升级为一条命令可判定的回归门禁。

新增/修改内容：
- 新增 `evaluation/baseline.py`：
  - `--write` 生成行为基线和 Workflow Golden fixture。
  - `--check` 重放当前行为并输出机器可判定差异。
  - 规范化本机项目绝对路径，保留路由、工具链、答案、证据、轨迹和评测指标。
- 新增 `evaluation/baseline-v1.json`：
  - 冻结 4 套 suite、193 条 case 的逐条结果。
  - 冻结 API 响应字段、五个 Tool 成功字段、三条代表性 Workflow 和优化实验摘要。
- 新增 `tests/contracts/test_tool_contracts.py`：
  - 覆盖五个 Tool 的 success 和 not_found 契约。
  - 覆盖 ethtool 空依赖属于成功结果的边界语义。
- 新增 `tests/contracts/test_api_contracts.py`：
  - 固定 AgentQueryResponse 字段、端点函数语义和 Health Schema。
- 新增 `tests/golden/agent_outputs.json` 和 `tests/golden/test_workflow_golden.py`：
  - 覆盖单工具成功、Hybrid 成功和 not_found 三类完整执行输出。
- 新增 `docs/architecture-v2.md`：记录当前真实架构、模块职责和兼容边界。
- 新增 `docs/api-contract.md`：记录请求、响应、Tool Observation 和向后兼容规则。
- 更新 `README.md`：增加基线验证、测试、显式重写基线命令和文档导航。

添加后的影响：
- 后续修改 Router、Planner、Workflow、Tool 或评测时，可以自动发现字段、工具顺序、答案、轨迹或指标回归。
- Step 18 可以在明确兼容边界下增加结构化 Evidence/Citation，不需要猜测旧接口行为。
- 测试覆盖从原有 6 条扩展为 24 条，增加 Tool 失败路径、API Schema 和完整 Workflow 输出保护。
- 测试命令统一使用 `-p no:cacheprovider`，避免当前环境的 pytest 缓存收尾挂起。

验证结果：
- `python -B evaluation/baseline.py --write`：成功生成基线，冻结 193 条评测。
- `python -B evaluation/baseline.py --check`：通过，当前行为与 baseline-v1 完全一致。
- `python -B -m pytest -q -p no:cacheprovider`：24 passed，0 failed。
- 四套评测合计 193 条，0 个 bad case，tool routing、task success、answer grounding 和 answer accuracy 均为 1.0。
- 本步骤没有修改 Router、Planner、Tool 或回答生成运行时逻辑，原 `/agent/query` 字段继续保留。

下一步：
- Step 18：Evidence 与 Citation 事实证据层。

## Step 18：Evidence 与 Citation 事实证据层

当前问题：
- Step 17 之前的 `evidence` 主要是 JSON 或文档的来源路径，只能说明 Tool 查询过哪个文件，不能定位答案中的包名、版本号和依赖边来自哪条具体记录。
- 五个 Tool 的业务输出结构不同，没有统一的 Observation Schema，也没有稳定 Evidence ID。
- Hybrid Plan 能合并路径字符串，但不能按事实记录去重，也没有 Citation 引用完整性检查。
- `not_found` 结果仍携带旧数据源路径，容易与“存在事实证据”混淆。

为什么要添加：
- 为后续 Step 19 Verifier 提供可以逐项校验的事实基础。
- 让答案中的包、版本、依赖、组件归属和文档内容可以追溯到具体 Evidence。
- 为 Trace、Feedback 和受控自进化提供稳定、可关联的证据 ID。
- 保持 V1 API 兼容，避免 Evidence 升级破坏 Step 17 冻结的行为。

新增/修改内容：
- 新增 `app/evidence/models.py`：定义 Evidence、Citation 和 ToolObservation，Evidence ID 使用确定性 SHA-256 派生。
- 新增 `app/evidence/normalizer.py`：为五个 Tool 建立来源映射，生成 `evidence_items` 和统一 `normalized_observation`。
- 新增 `app/evidence/validator.py`：校验 Evidence ID 唯一性、Schema 和 Citation 引用关系。
- 新增 `app/tools/base.py`：统一执行 Tool、记录 latency_ms 并接入 EvidenceNormalizer。
- 修改五个 Tool：保留原有顶层字段，新增 `evidence_items`、`error`、`metadata` 和 `normalized_observation`。
- 修改 `app/agent/workflow.py`：Hybrid Plan 按 Evidence ID 合并去重，新增 `evidence_items`、`citations` 和 `evidence_count`。
- 修改 API、Memory 和 Demo：API Schema 增加三项字段，持久化记录保留结构化证据，Demo 展示 Citation 数量和详情。
- 新增 `evaluation/evidence_eval.py`、`GET /evaluation/evidence` 和 `--suite evidence`。
- 新增 `tests/test_evidence.py`，覆盖稳定 ID、五 Tool normalization、not_found、Hybrid 去重、Citation 完整性和 193 条专项评测。
- 新增 `docs/evidence-citation.md` 和 `evaluation/evidence_report.md`，同步更新 Architecture、API Contract、README 和 Project Summary。
- 修改 Step 17 baseline/Golden 比较：旧键和值必须一致，但允许向后兼容新增字段；`baseline-v1.json` 本身未被重写。

实验中发现的问题：
- 第一版 Citation coverage 计算中，分子计入了“不要求 Citation 的 not_found 用例”，分母却只统计需要 Citation 的成功用例。
- 该错误导致 Citation coverage 出现 101.58% 的不可能结果。
- 修复后，分子和分母均只统计 `citation_required=true` 的用例，最终指标恢复为 100%。

添加后的影响：
- Agent 回答中的结构化事实现在可追溯到具体软件包记录、依赖边、版本记录、组件映射或文档 Chunk。
- 相同事实跨重复执行产生稳定 Evidence ID；Hybrid 多工具结果不会重复引用同一 Evidence。
- `not_found` 不生成结构化 Evidence 或 Citation，避免伪造来源。
- Step 17 的原字段、Tool 顺序、答案和 193 条评测继续受到 baseline-v1 保护。

验证结果：
- Evidence 专项评测覆盖原有 193 条 case。
- Citation coverage：100%。
- Evidence normalization success：100%。
- Citation correctness：100%。
- not_found without Citation：100%。
- Unsupported structured facts：0。
- Evidence bad cases：0。
- Step 17 `baseline-v1 --check`：通过，向后兼容。
- 完整自动化测试：32 passed，0 failed。

面试表达：
> 我没有把 Tool 返回的文件路径直接当作完整证据，而是设计了统一 Evidence Schema，把 package、dependency edge、version record、component mapping 和 document chunk 转成带稳定 ID 的事实对象。Hybrid Plan 会按 Evidence ID 合并去重，最终回答通过 Citation 引用证据；同时保留旧 API 字段，并用 193 条回归集验证 Citation coverage、normalization 和 unsupported facts。

下一步：
- Step 19：确定性 Verifier 与部分成功语义。

## Step 19：确定性 Verifier 与部分成功语义

当前问题：
- Step 18 已能证明事实来自哪条 Evidence，但回答生成后仍会直接返回，没有在线检查 Evidence 是否被正确使用。
- 伪造版本、依赖方向颠倒、无效 Citation 和 Hybrid 漏项只能在离线评测中发现。
- 原有 `success: bool` 只能表达“全部成功”或“不是全部成功”，无法区分部分成功、正常无记录和系统失败。
- 如果直接加入自由重试，可能产生无限循环、重复 Tool 调用和不可复现行为。

为什么要添加：
- 将错误发现从离线评测前移到 API 响应返回之前。
- 对 Hybrid 任务保留已经得到的可靠结果，同时明确说明缺失子任务。
- 为后续 Trace、Feedback、Bad-case 分类和策略候选提供结构化 Verification Issue。
- 通过一次确定性答案重组限制修复范围，不让 Agent 自动重新规划或修改事实。

新增/修改内容：
- 新增 `app/agent/verifier.py`：
  - 定义 VerificationIssue 和 VerificationResult。
  - 实现 11 条确定性规则：plan complete、arguments、evidence integrity、citations valid、citation coverage、version grounding、dependency direction、status consistency、not_found semantics、hybrid completeness 和 answer nonempty。
  - 实现 `aggregate_execution_status()` 四态聚合。
  - 实现最多一次、仅重组答案的 `verify_and_repair()`。
- 修改 `app/agent/workflow.py`：
  - 在答案返回前执行 Verifier。
  - 新增 `execution_status` 和 `verification`。
  - 将 VerificationResult 附加到 answer_generation trajectory 节点。
- 保留旧 `success: bool` 语义，确保 Step 17 API 兼容。
- 修改 API Schema、Memory 和 Demo：
  - API 返回四态状态和 VerificationResult。
  - Demo 将原 Success 指标升级为 Execution Status，并展示 Verification 详情。
- 新增 `evaluation/verifier_cases.json`：覆盖 11 类故意注入错误。
- 新增 `evaluation/verifier_eval.py`、`--suite verifier` 和 `GET /evaluation/verifier`。
- 新增 `tests/test_verifier.py`：覆盖规则数量、正常通过、partial_success、四态聚合、API Schema 和专项指标。
- 新增 `docs/verifier-partial-success.md` 和 `evaluation/verifier_report.md`，同步更新 Architecture、API Contract、Evidence 文档、README 和 Project Summary。

实验中发现的问题：
- 第一版依赖解析把 `.so` 中的句点当作句末，导致 `libhttp.so` 和 `libpcap.so` 被截断，合法 Hybrid 回答被误判为 wrong dependency direction。
- 修复方式是先按真正的句子边界切分，再单独去除末尾标点，保留组件扩展名。
- 第一版 missing-step 注入只删除了一个展开后的 `version_compare` Observation，但 release 计划会将一个步骤展开成两个包的调用，因此仍有同类 Tool 存在。
- 修复方式是删除目标计划步骤产生的全部同类 Observation，正确模拟整个步骤缺失。

部分成功语义：
```text
全部 success                         -> success
success + not_found/partial_success  -> partial_success
全部 not_found                       -> not_found
包含 failed                          -> failed
```

添加后的影响：
- Agent 能在返回前拦截无证据版本号、错误依赖方向、缺失 Tool、无效 Citation 和不完整回答。
- Hybrid 中单个子任务缺数据时返回 `partial_success`，答案保留成功结果并说明 Missing records。
- 修复最多执行一次，且只使用原 Tool Observation 重新组合答案，不增加 Tool 调用次数。
- Verification Issue 可以作为 Step 21 Trace 和 Step 22 Bad-case Loop 的直接输入。

验证结果：
- Verifier 确定性规则：11 条。
- Injected error detection：100%（11/11）。
- 原有合法请求：193 条。
- False rejection rate：0%。
- Partial-success classification：100%。
- Invalid Citation detection：100%。
- Single deterministic repair：通过。
- Maximum repair count：1。
- Verifier bad cases：0。
- 完整自动化测试：38 passed，0 failed。
- Step 17 baseline-v1：193 条兼容检查通过。
- 原四套主评测：193 条，0 bad case，四项核心指标保持 1.0。

面试表达：
> Evidence 只能证明事实来源，还不能保证回答正确使用了事实。因此我在返回链路增加了确定性 Verifier，对计划完整性、版本号、依赖方向、Citation、not_found 和 Hybrid 完整性进行在线检查。我还设计了 success、partial_success、not_found、failed 四态语义，并把修复限制为基于原 Observation 的一次答案重组，避免无限重试和不可复现执行。

下一步：
- Step 20：Hybrid RAG 检索升级。

## Step 20：Hybrid RAG 检索升级

当前问题：
- 原有 RAG 只使用英文词元重叠和简单来源加权，中文工程表达、别名和中英混合查询容易无召回。
- 文档片段缺少稳定 `chunk_id`、`document_id`、`section` 和版本元数据，Evidence 只能依赖标题与内容哈希定位。
- 检索结果没有 BM25、融合排序和重排的分项得分，无法解释某个 Chunk 为什么排在前面。
- 原 193 条 Agent 评测验证的是任务结果，不包含 Recall@K、MRR 和无答案识别率，无法单独证明 RAG 检索质量。

为什么要添加：
- 将“能查到文档”升级为可标注、可比较、可复现的检索实验。
- 在不购买 Embedding 或 LLM Token 的情况下，用 BM25、RRF 和确定性特征完成离线 Hybrid RAG。
- 为研发文档中的中英文术语、软件包别名、发布版本和来源过滤提供更稳健的召回能力。
- 为后续 Trace、Feedback 和 Reranker 权重优化提供稳定 Chunk ID 和分项得分。

新增/修改内容：
- 新增 `app/rag/chunker.py`：定义标准 DocumentChunk，并按 Markdown 标题生成稳定 Chunk ID。
- 新增 `app/rag/bm25.py`：实现无第三方依赖的 BM25，支持标题/正文权重、Top-K、来源和版本过滤。
- 新增 `app/rag/hybrid.py`：融合 Legacy Top20 与 BM25 Top20，使用 RRF（k=60）合并候选。
- 新增 `app/rag/reranker.py`：按软件包、release、标题、来源意图和 section 做确定性重排。
- 扩展 `app/rag/embedding.py`：增加抓包、安全通信、网口、管理面等中英文工程术语映射。
- 重构 `app/rag/retriever.py`：支持 `legacy/bm25/hybrid` 三种模式，统一输出完整 Chunk 与分项分数，同时保留 V1 字段。
- 修改 `RAGRetrieverTool`：版本过滤前移至召回阶段，并返回 `retriever_mode`。
- 修改 EvidenceNormalizer：文档 Evidence 的 `source_id` 优先使用稳定 Chunk ID，metadata 记录检索模式和分项分数。
- 新增 30 条 `evaluation/rag_cases.json` 标注集与 `evaluation/rag_eval.py`，计算 Recall@3、Recall@5、MRR、无答案识别率和 Citation correctness。
- 新增 `--suite rag`、`GET /evaluation/rag`，Dashboard 增加三种检索模式消融表。
- 新增 `tests/test_hybrid_rag.py`，覆盖 Chunk Schema、三模式、中文别名、空查询、过滤、排序稳定性、无答案、Citation 和阈值回归。
- 新增 `docs/hybrid-rag.md` 与 `evaluation/hybrid_rag_report.md`，并同步 README、Architecture、API Contract 和 Project Summary。

实验中发现的问题：
- 第一版将 Hybrid 设为默认后，虽然新检索指标提升，但旧 Agent 的第三条 RAG 证据顺序发生变化，Step 17 baseline 检测到回答文本回归。
- 第一版 `RAGOnlyProxy` 随默认检索器升级，成功率从 57.06% 变为 57.65%，导致历史 Baseline 与新方法对比口径混在一起。
- 修复方式：运行时默认继续使用 Legacy 保护 V1 契约，允许通过 `SOFTWARE_AGENT_RAG_MODE=hybrid` 显式启用；RAG 消融评测始终显式指定模式；历史 RAGOnlyProxy 固定为 Legacy。
- Legacy 对 4 条中文或混合查询召回失败；BM25 查询扩展修复了这些 Bad Case，Hybrid 的确定性重排还将“管理面 web 服务组件”的手册 Chunk 提升到第一名。

添加后的影响：
- 文档证据现在可以稳定定位到 `software_manual:tcpdump` 等 Chunk，而不是依赖易变化的内容哈希。
- 每个检索结果都能解释 Legacy、BM25、RRF、Reranker 和 final score。
- 中文工程表达可以映射到对应英文软件资产术语，模糊查询鲁棒性提高。
- 不相关、空查询和不存在 release 会返回空结果，避免把弱相关文档当作事实证据。
- Step 17 的旧 Agent 回答和离线 Baseline 口径保持不变；新模式可以独立启用和评测。

验证结果：
- RAG 标注集：30 条，其中可回答 24 条、无答案 6 条。
- Legacy：Recall@3 83.33%，Recall@5 83.33%，MRR 79.17%。
- BM25：Recall@3 100%，Recall@5 100%，MRR 95.83%。
- Hybrid：Recall@3 100%，Recall@5 100%，MRR 95.83%。
- Hybrid no-answer accuracy：100%；Citation correctness：100%。
- Hybrid 相比 Legacy 的核心指标无回退，全部 Step 20 验收阈值通过。
- 完整自动化测试：50 passed，0 failed。
- Step 17 baseline-v1：193 条兼容检查通过。

面试表达：
> 原始 RAG 只是词元重叠，我先统一了文档 Chunk Schema 和稳定 ID，再实现标题加权 BM25，把 Legacy 与 BM25 的 Top20 用 RRF 融合，并通过软件包、release 和来源意图做确定性重排。随后构建 30 条带相关 Chunk 标注的中英文评测集，用 Recall@K、MRR、无答案识别率和 Citation correctness 做消融。Hybrid 的 Recall@3 从 Legacy 的 83.33% 提升到 100%，MRR 从 79.17% 提升到 95.83%，同时用配置开关和 baseline 保护旧接口行为。

下一步：
- Step 21：多轮 Context 与增强 Trace。

## Step 21：多轮 Context 与增强 Trace

当前问题：
- Step 20 之前每次 `run_agent()` 都是独立单轮请求，“它的依赖”“再比较版本”等省略实体的追问无法知道目标软件包。
- 原 trajectory 缺少 `trace_id/session_id/parent_trace_id/policy_version`，不能把同一会话的多轮执行串成可审计链路。
- Tool Observation 虽有单次延迟，但没有统一进入可重放 Trace；原记录也没有明确区分原始输入和上下文解析后的输入。
- trajectory 的 planning 节点包含 route reason，不适合作为后续 Feedback、审计和 Replay 的最小隐私记录。
- Session 隔离、上下文清除、容量限制和跨会话污染此前没有专项评测。

为什么要添加：
- 支持研发人员自然地连续执行“查包 -> 查它的依赖 -> 再比较版本”，减少重复输入完整实体。
- 为 Step 22 Feedback 与 Bad-case 归因建立统一的 trace_id、策略版本、Evidence、Verification 和执行状态关联。
- 让一次执行能够按 trace_id 重建输入并隔离重放，便于复现错误和比较策略候选。
- 用明确的 Session 边界和最小化字段控制上下文污染与隐私风险。

新增/修改内容：
- 新增 `app/agent/context.py`：定义 AgentContext、ContextResolver 和线程安全的 SessionRepository。
- Context 只继承 package、release、component；显式实体优先，默认最多保存 100 个 Session、20 条近期 Turn 摘要和 8 个软件包实体。
- 修改 `run_agent()`：支持可选 `session_id`，执行前解析上下文，执行后更新 Session，并返回 resolved_query 和 inherited_context。
- 新增 `app/agent/trace.py`：定义 trace-v1、TraceRepository、TraceRecorder 和 ReplayReader。
- trace-v1 记录 trace/session/parent ID、策略版本、原始与解析后输入、Plan、Tool 输入/状态/延迟、Evidence ID、Citation、Verification、输出和总延迟，不记录内部 Thought。
- Trace 默认保存在有容量限制的进程内 Repository；`persist_trajectory=true` 时追加到 `data/traces.jsonl`，旧 `trajectories.jsonl` 继续兼容。
- 新增 Session 查询/清除、Trace 查询、Replay Input 和隔离 Replay API。
- API Response 以兼容新增方式加入 session、trace、context、policy 和 replay 字段。
- Demo 增加固定 Session 输入、清除按钮和三步多轮演示；Dashboard 增加 Context/Trace 指标。
- 新增 `evaluation/context_cases.json`：8 个多轮会话、18 个 Turn、10 个实体继承检查和 5 个跨 Session 隔离探针。
- 新增 `evaluation/context_eval.py`、`--suite context` 和 `GET /evaluation/context`。
- 新增 `tests/test_context_trace.py`：覆盖实体继承、显式覆盖、Session 隔离、清除、容量、Trace 隐私、父子链、JSONL、Replay 和 API Schema。
- 新增 `docs/context-trace.md` 与 `evaluation/context_trace_report.md`，同步更新 README、Architecture、API Contract 和 Project Summary。

实验中发现的问题：
- 第一版 Trace 完整性检查直接在序列化文本中搜索 `thought`，把隐私声明字段 `stores_internal_thought=false` 自身误判为内部 Thought。
- 修复方式是递归检查是否真的存在名为 `thought` 的字段，同时保留明确的 `stores_internal_thought=false` 审计声明。
- 第一版通用规则看到“版本”就继承最近 package，导致“这个版本的发布说明”同时继承 release 和 tcpdump，解析结果多出不必要实体。
- 修复方式是让明确 release 指代优先于通用 package follow-up，只继承 release；显式 package 仍具有最高优先级。
- tcpdump 多轮评测初始 fixture 写成旧版本 4.90，但模拟源数据是 4.95；该问题属于测试数据与事实源不一致，已按 `versions.json` 修正。

添加后的影响：
- 同一 Session 内可连续追问 package 依赖和版本变化，用户无需重复软件包名称。
- 不同 Session 完全隔离；清除 Session 后不会继续继承实体。
- 每轮结果都有唯一 trace_id，并通过 parent_trace_id 形成会话执行链。
- 任意内存中或已持久化 Trace 都能重建原始输入、解析后输入、继承实体和策略版本，并在新 Session 隔离重放。
- 后续 Feedback 可以直接关联 trace_id、Evidence ID、Verification Issue、策略版本和 Tool 延迟，不再依赖解析整份旧 trajectory。
- 旧 API 字段、旧 trajectory 和 193 条单轮评测语义保持兼容。

验证结果：
- 多轮会话：8 组，18 个 Turn，10 个上下文依赖 Follow-up。
- Multi-turn entity consistency：100%。
- Cross-session leak count：0，leakage rate：0%。
- Trace completeness：100%。
- Replay input reconstruction：100%。
- Context/Trace Bad Case：0。
- 完整自动化测试：59 passed，0 failed。
- Step 17 baseline-v1：193 条兼容检查通过。
- FastAPI 后台服务健康检查通过；真实 HTTP 多轮请求正确继承 openssl，parent_trace_id 链接正确，Replay Input 重建成功。

隐私与保留边界：
- Context 只保存任务必要实体和最近 Turn 摘要，不保存内部 Thought、凭据或任意个人信息摘要。
- Session 清除不会删除已经持久化的审计 Trace；生产环境需要为 JSONL 配置独立保留和删除策略。
- Replay 使用 resolved_query 在新 Session 执行，不修改原 Session。

面试表达：
> 我把原来的单轮 trajectory 升级为 Session Context 和 trace-v1 两层。Context 只继承 package、release、component，并通过显式实体优先、Session 隔离和容量限制控制污染；Trace 则关联 trace_id、parent_trace_id、策略版本、Tool 延迟、Evidence 和 Verification，不记录内部 Thought。我构建了 8 组多轮会话和跨 Session 污染集，实体一致性、Trace 完整率和 Replay 输入重建率均为 100%，跨会话泄漏为 0，同时旧 193 条单轮基线保持兼容。

下一步：
- Step 22：Feedback 与受控 Bad-case Loop。

## Step 22：Feedback 与受控 Bad-case Loop

当前问题：
- Step 21 已有完整 trace-v1，但用户无法针对某条 Trace 提交结构化反馈，Bad Case 与执行证据之间没有持久关联对象。
- 原 `run_bad_case_analysis()` 只能在离线报告里给出文字建议，不能按错误类型聚合反馈、生成可执行候选或隔离回放。
- Router/Planner 优化仍需要人工直接修改 Python，无法在改动前证明候选能修复问题且不会破坏 193 条基线。
- 缺少最小反馈阈值、候选安全作用域、人工审批状态和“未审批不可生效”的系统门禁。
- 如果直接实现自动改代码或自动上线，会引入测试门槛篡改、权限扩大、不可回滚和策略漂移风险。

为什么要添加：
- 把 Trace、用户反馈、Bad Case 归因、优化候选和评测结果串成可审计闭环。
- 让系统自动提出“配置层怎么改”，但把源码修改、候选审核和上线权保留给人。
- 在候选进入评审前自动运行关联 Replay 和冻结回归集，量化修复数、新回归、核心指标和延迟。
- 为 Step 23 的策略版本化、灰度、监控和回滚准备已经通过门禁的候选资产。

新增/修改内容：
- 新增 `app/feedback/models.py`：定义 FeedbackRecord、PolicyCandidate、合法 Issue 和候选状态。
- 新增 `app/feedback/repository.py`：实现有容量限制的 Feedback/Candidate Repository。
- 新增 `app/feedback/classifier.py`：FeedbackObserver 按 trace_id 读取 query、Tool、policy、Evidence 和 Verification；BadCaseClassifier 输出归因层、trigger 和 fingerprint。
- 新增 `app/feedback/policy.py`：实现 CandidateConfigValidator、CandidatePolicyRunner 和 CandidateEvaluator。
- 新增 `app/feedback/service.py`：实现提交、聚合、提议、评测、人工审核和激活阻断状态机。
- 当前 Proposer 只支持 `wrong_tool` Router Hook；至少 3 条同 fingerprint 负反馈才能生成候选。
- Candidate 安全作用域明确禁止修改 Python 源码、数据集、测试断言、权限和发布门槛。
- 候选评测运行关联 Bad Case 和冻结的 193 条 standard/challenge/robustness/large case，并比较路由、任务成功、Grounding、答案准确率和延迟。
- 发布门禁包括：候选得分提升、至少修复 2 条、零回归、核心指标不下降、延迟增加不超过 15%、配置作用域合法。
- 状态机实现 `draft -> replaying -> pending_review -> approved/rejected`；approved 仍保持 `active=false`。
- 新增 `/feedback`、`/candidates/*` 和 `/evaluation/feedback` API；`/activate` 在 Step 22 固定返回 409。
- Demo 可对当前 trace_id 提交 wrong_tool 反馈；Dashboard 展示候选前后得分、修复数、回归、延迟和状态。
- 新增 `evaluation/feedback_cases.json`：三条 `prerequisites` 路由盲区。
- 新增 `evaluation/feedback_loop_eval.py` 和 `--suite feedback`。
- 新增 `tests/test_feedback_loop.py`，覆盖 Trace 关联、三条阈值、候选 Schema、全量门禁、人工审核、激活阻断和非法作用域。
- 新增 `docs/controlled-feedback-loop.md` 和 `evaluation/controlled_feedback_report.md`，同步更新 README、Architecture、API Contract 和 Project Summary。

实验 Bad Case：
- `openssl prerequisites`、`nginx prerequisites`、`tcpdump prerequisites` 中的 `prerequisites` 不在当前依赖关键词表中，三条都被基线 Router 选择为 `package_search`。
- 三条 Trace 分别提交 `wrong_tool` 反馈，期望 Tool 为 `dependency_analysis`；Classifier 将它们聚合为同一 fingerprint：`wrong_tool:dependency_analysis:prerequisites`。
- 只有两条反馈时 Candidate Proposer 正确阻断；第三条加入后生成 `prerequisites -> dependency_analysis` Router Hook。
- 第一轮测试出现的唯一失败是测试代码把 FeedbackRecord dataclass 当作字典下标读取，属于测试访问方式错误，不是运行时缺陷；改为属性读取后通过。

添加后的影响：
- 用户反馈现在可追溯到具体 Trace、Session、策略版本、当前 Tool、Evidence 和 Verification。
- 同类错误达到阈值后可自动形成结构化配置候选，不需要系统修改 Python 源码。
- 候选可在隔离环境修复关联 Bad Case，并用原 193 条评测阻止行为回归。
- 候选必须经过人工 approve/reject；即使 approved 也不会自动进入 Agent，避免“评测通过即上线”。
- Step 23 可以直接接收 approved candidate，增加版本号、灰度比例、线上监控和回滚，不必重新设计 Feedback 数据链。
- 原 Router 和 `/agent/query` 默认行为没有改变，Step 17 baseline 继续保护旧契约。

验证结果：
- Feedback/Trace linkage：100%。
- Bad Case classification accuracy：100%。
- 最小三条反馈阈值：正确执行。
- 关联路由准确率：Baseline 0% -> Candidate 100%。
- 修复 Bad Case：3 条。
- 冻结回归集：193 条；Regressed cases：0。
- Candidate 核心指标：Tool Accuracy、Task Success、Grounding、Answer Accuracy 均保持 100%。
- Added latency ratio：0% 测得增幅，低于 15% 门槛。
- Candidate 状态：pending_review；active=false。
- 完整自动化测试：68 passed，0 failed。
- Step 17 baseline-v1：193 条兼容检查通过。
- FastAPI 真实 HTTP 闭环通过：提交 3 条 Trace Feedback、生成 Candidate、执行 193 条回归后停在 pending_review，fixed=3、regressions=0、active=false。

安全边界：
- Agent 不得自动修改 Python、模拟数据、测试断言、权限或发布门槛。
- Candidate Runner 只在隔离 Session/Trace Repository 中执行，不写入当前默认策略。
- `pending_review` 只能由人类 reviewer 批准或拒绝。
- Step 22 不提供激活能力；真正发布必须等待 Step 23 的策略版本化、灰度和回滚。

面试表达：
> 我没有让 Agent 自动改代码，而是实现了受控 Feedback Loop。用户反馈先绑定 trace_id，系统从 Trace 中归因 wrong_tool、Tool failure、Grounding 或 Verifier 问题；同类反馈达到阈值后只生成配置型 Router Hook。候选必须隔离回放关联 Bad Case，并跑完整 193 条回归和延迟门禁。这次实验把 prerequisites 路由准确率从 0% 提升到 100%，修复 3 条且零回归，但候选只进入 pending_review，人工批准后仍不自动激活，为后续灰度和回滚保留控制权。

下一步：
- Step 23：策略版本化、灰度、回滚与工程交付。

## Step 23：策略版本化、灰度、回滚与工程交付

当前问题：
- Step 22 的 Candidate 即使通过 193 条回归并经人工批准，也没有受控进入运行时的路径，只能停留在 `approved + active=false`。
- 如果通过直接修改 Router 源码上线，策略版本、实际生效范围和历史 Trace 无法准确归因，也难以快速恢复旧行为。
- 缺少稳定灰度分流；随机分配会让同一 Session 在新旧策略间跳动，导致多轮行为不一致。
- 缺少 control/rollout 指标比较和自动回滚门槛，评测通过后的线上退化无法及时止损。
- 项目此前没有把运行依赖、开发依赖、CI、容器、发布手册和回滚手册组合成可重复的工程交付物。

为什么要添加：
- 把“候选优化实验”连接到“可审计发布”，形成 Feedback、评测、审批、灰度、监控、回滚的完整闭环。
- 用不可变版本和 Trace 归因回答某次请求到底使用了哪套策略，便于复现问题和比较实验。
- 让新策略只影响可控比例的 Session，并保留稳定策略作为同期 control，降低发布风险。
- 通过确定性指标门禁自动恢复父策略，证明 Agent 优化不是一次性改代码，而是可运营、可回退的系统能力。
- 用 Docker 和 CI 让 API、测试与评测可以在独立环境重复执行，达到开源项目交付要求。

新增/修改内容：
- 新增 `app/policy/models.py`、`repository.py`、`engine.py`、`monitor.py`、`service.py`，实现 PolicyVersion、配置校验、版本仓库、灰度分配、监控和发布服务。
- 初始稳定策略为 `deterministic-policy-v1`；approved Candidate 可生成 `policy_vN`，保留父版本、来源 Candidate、发布人、时间、配置和状态。
- 使用 `session_id` 的 SHA-256 稳定桶进行灰度；比例不变时同一 Session 始终属于同一 cohort。
- `run_agent()` 在规划前解析策略；显式测试 Plan 仍优先，策略规则次之，未命中则完全保持 V1 Router 行为。
- Agent 响应、Memory 和 trace-v1 增加 `policy_assignment`，记录 policy、version、cohort、bucket 和 rollout percentage。
- PolicyMonitor 分别统计 control/rollout 的样本数、成功率和平均延迟；最少各 5 条后，rollout 成功率低于 80% 或落后 control 超过 10 个百分点时自动回滚。
- 支持 start rollout、调整比例、promote、manual rollback、automatic rollback 和 deprecate；回滚会停用来源 Candidate。
- 默认策略状态持久化到 `data/policy_state.json`，进程重启后可重建 stable、rollout 和历史版本。
- 新增 8 个 Policy API 和 `/evaluation/policy`；Demo 显示当前策略与 cohort，Dashboard 显示灰度/回滚实验。
- 新增 `evaluation/policy_eval.py`、`evaluation/smoke.py`、`examples/policy_rollout_demo.py` 和策略评测报告。
- 新增策略、交付测试；新增 runtime/dev requirements、Dockerfile、Compose、Ruff/Mypy 配置和 GitHub Actions。
- 新增 `docs/policy-rollout.md`、发布 Runbook、回滚 Runbook，并同步 README、Architecture、API Contract 和 Project Summary。

实验中发现的问题：
- Ruff/Mypy 默认尝试在中文工作区创建隐藏缓存，但当前受限文件系统拒绝该写入；Ruff 改为 `--no-cache`，Mypy 改用 Runner 临时目录。
- Mypy 从 Policy/Feedback 递归扫描到早期 RAG 模块，混淆了 Step 23 的类型验收边界；CI 对本阶段模块使用 `--follow-imports=skip`，运行测试仍覆盖完整跨模块行为。
- Policy Repository 的 `_load()` 初版依赖构造器保证 path 非空，静态检查无法完成类型收窄；增加显式 path 防御检查后通过。
- 真实 HTTP 发布首次在受限桌面环境返回 500，日志定位为后台进程无权创建 `data/policy_state.json.tmp`；增加 `SOFTWARE_AGENT_POLICY_STATE_PATH` 后，可把状态落到外部卷或可写临时目录，容器默认路径不变。
- 本机没有安装 Docker CLI，因此无法在当前机器实际执行镜像构建；Dockerfile、Compose 和 CI 构建步骤由交付契约测试检查，真正镜像构建交给 GitHub Actions 或有 Docker 的环境。

添加后的影响：
- approved Candidate 现在可以在不修改 Agent 源码的前提下进入 1%-100% 灰度，并可独立调整或提升。
- 同一 Session 不会在策略间漂移；control 和 rollout 可以同期比较而不是拿不同时段结果硬对比。
- 任意 Trace 能定位具体策略版本，即使该版本已经回滚，历史执行仍可审计和重放。
- 失败策略可自动或手动回到父版本，Candidate 同步失活，下一次请求立即恢复稳定行为。
- 工程交付包含 API、CLI、评测、测试、静态检查、容器和运维手册，项目从实验原型升级为可发布服务。

验证结果：
- 灰度分配样本：1,000 个 Session；rollout 184 个，观测比例 18.4%，位于 20% 灰度的 15%-25%验收区间。
- 相同 Session 重复分配稳定率：100%。
- control 使用 `deterministic-policy-v1 + package_search`；rollout 使用 `policy_v2 + dependency_analysis`。
- Trace 策略版本定位：通过。
- 注入 control 成功、rollout 失败后自动回滚：通过；恢复 `deterministic-policy-v1`。
- Router、Planner、Workflow 回滚前后源码哈希一致：通过。
- Policy Evaluation：12/12 门禁通过，Bad Case 0。
- CI Smoke：核心 12 条 + Policy 全门禁通过。
- Ruff syntax/undefined-name gate：通过。
- Mypy Policy/Feedback boundary：通过。
- 完整自动化测试：82 passed，0 failed；Step 17 冻结基线 193 条全部兼容。
- 真实 HTTP 闭环：Candidate 修复 3 条、回归 0 条，经人工 approve 后以 20% 发布为 `policy_v2`。
- HTTP control 为 `deterministic-policy-v1 + package_search`，rollout 为 `policy_v2 + dependency_analysis`；历史 Trace 保留 `policy_v2`。
- 注入成功率 100% 的 control 与成功率 20% 的 rollout 后，服务自动回滚；新分配立即恢复 v1。
- Docker 本机实构建：未执行，原因是当前环境没有 Docker CLI。

Docker 部署补充记录（2026-08-23）：
- 已确认 WSL 2 版本满足 Docker Desktop 要求，并从 Docker 官方地址下载、验证了 x86_64 安装包签名。
- 安装阶段发现本机磁盘空间不足，按项目安排暂停本地 Docker Desktop 安装与真实镜像构建。
- 已删除约 628.7 MB 的临时安装包；系统未残留 Docker Desktop 目录或安装进程。
- 当前项目目录尚未初始化 Git 仓库，因此暂时不能改用 GitHub Actions 远程构建；Dockerfile、Compose、CI Build 和交付测试继续保留。
- 该延期不影响 Agent、Policy、RAG、Evaluation 和 FastAPI 后续开发，但在真实构建通过前，项目表述保持为“完成容器化配置”，不表述为“镜像部署已验证”。

## Step 24：可选真实 LLM Provider 与离线/在线双模式评测

当前问题：
- 项目已有 `llm_router.py` 和 `/agent/query-with-plan`，但只能接收外部预先生成的 JSON，尚未形成真正的 Provider 调用边界。
- 现有“LLM Router”表述容易让人误以为默认执行会调用外部模型；实际上当前 193 条结果全部来自离线确定性 Planner。
- 如果直接把在线 SDK 写入 Workflow，会让 API Key、网络错误、超时、非法 Tool 和调用费用侵入稳定主链。
- 缺少 Offline 与 Online 的同口径对比，也没有证明在线失败时 Tool Executor 不会执行未经校验的 Plan。
- 用户当前没有要求购买 Token，因此不能把付费 API 变成项目运行和评测的前置条件。

为什么要添加：
- 把“未来可以接 LLM”的接口占位升级为可运行的 Provider Architecture，同时保留零成本、可复现的默认模式。
- 让模型只拥有 Plan 提议权，Tool、Evidence、Verifier、Policy 和 Trace 的控制权继续留在确定性系统中。
- 对缺 Key、未启用、超时、API 错误、Malformed JSON 和未知 Tool 建立统一失败语义与安全降级。
- 在真实付费实验前先用 Mock Online 验证集成正确性，区分“系统接入能力”和“真实模型效果”。
- 为后续小预算真实 Provider A/B 实验准备调用次数、Token Usage、Latency 和 Trace 归因字段。

新增/修改内容：
- 新增 `app/providers/`：ProviderRequest/Result、Protocol、Settings、Offline Provider、OpenAI Provider、Mock Provider、PlannerGateway 和 Provider-aware Service。
- 默认 Provider 为 `offline`，不访问网络、不消耗 Token；`auto` 读取环境配置，`openai` 必须显式启用。
- OpenAI Provider 使用 Responses API、`store=false` 和 JSON Schema Structured Outputs，只返回 Agent Plan。
- 在线 Plan 进入主链前继续通过 `parse_llm_plan()` 本地二次校验；未知 Tool 或非法 Step 不能到达 Executor。
- `SOFTWARE_AGENT_ENABLE_ONLINE_LLM=true` 与 `OPENAI_API_KEY` 必须同时满足；Key 只由 SDK 读取，状态 API 只返回是否已配置。
- Provider timeout 限制为 1-60 秒，最大输出限制为 128-2000 Token，SDK 重试设为 0，保证一次 Agent 请求最多一次在线规划调用。
- `allow_fallback=true` 时在线失败切回 Offline Planner；关闭 fallback 时返回 503 且不执行 Tool。
- Agent Response、planner_source 和 trace-v1 增加 Provider、Model、Latency、Usage、Fallback 与 Error Type。
- 新增 `/agent/query-provider`、`/providers/status`、`/evaluation/provider`，Demo 增加 Planner Provider 选择，Dashboard 增加 Provider Dual Mode 指标。
- 在线 SDK 依赖独立放入 `requirements-online.txt`，默认 runtime 不增加 OpenAI 包与镜像体积。
- 新增 12 条 `evaluation/provider_cases.json`、`provider_eval.py`、专项报告、演示脚本和 Provider 测试。
- CI Smoke 加入 Provider 双模式评测；同步 README、Architecture、API Contract 和 Project Summary。

实验 Bad Case 与修复：
- 注入 `not-json`：本地 JSON Parser 标记 `invalid_plan`，切回 Offline Planner。
- 注入 `tool=shell`：Plan Validator 标记 `unknown_plan_tool`，未执行未知工具并切回 Offline。
- 注入 Timeout：ProviderResult 标准化为 `error_type=timeout`，切回 Offline，任务继续成功。
- 禁用 fallback 后注入 Timeout：Gateway 设置 `execution_allowed=false`，API 返回 503，Tool 调用数为 0。
- 环境变量 timeout 或 max tokens 非数字时，初版可能在服务导入阶段抛 ValueError；增加有界解析后恢复默认值或限制到最大值。
- Provider SDK 异常文本理论上可能包含敏感内容；增加 API Key 替换为 `[redacted]`，公开状态永远不返回 Key 值。

添加后的影响：
- 项目现在可以在同一 Agent 主链中切换 Offline 和可选 Online Planner，而 Tool/Evidence/Verifier 行为保持一致。
- 默认安装、测试、API 和 Dashboard 不需要 Token，不会产生外部调用费用。
- 在线输出不能绕过 Tool 白名单和 Plan Schema，模型无法直接运行 Shell、改策略或修改评测门槛。
- Trace 可以回答某次请求使用哪个 Provider/Model、是否 fallback、延迟和 Token 用量。
- 外部服务不可用时可以保持业务可用；严格场景也可以关闭 fallback，明确失败而不是悄悄改变语义。
- 后续真实模型实验只需安装 online requirements 和设置环境变量，不需要修改 Workflow 源码。

验证结果：
- Provider 标注集：12 条，覆盖 Package、Dependency、Version、Component、RAG 和 Hybrid。
- Offline/Mock-Online Plan parity：100%。
- Online Provider Metadata 进入 Trace：100%。
- Malformed JSON、Unknown Tool、Timeout 三类故障安全降级率：100%。
- Fallback 后任务成功率：100%。
- `allow_fallback=false` fail-closed：通过。
- 每条 Online Case 调用次数：1 次，无额外重试。
- Paid API calls：0。
- Provider 专项测试：8 passed，0 failed。
- 完整自动化测试：92 passed，0 failed；Step 17 冻结基线 193 条全部兼容。
- FastAPI 真实 HTTP：`/providers/status` 显示 online=false、key=false、secrets_exposed=false。
- Offline HTTP 请求：effective provider=offline、Token Usage=0、依赖工具执行成功。
- OpenAI 未启用且允许 fallback：requested=openai、effective=offline、reason=provider_unavailable，Trace 同步记录 offline。
- OpenAI 未启用且禁止 fallback：HTTP 503，未进入 Tool Executor。
- `/evaluation/provider`、Demo 和 Dashboard HTTP 验收通过；服务以 PID 3504 在 8000 端口运行。

边界与诚实表述：
- Mock Online 只证明 Provider 接口、校验、降级和 Trace 正确，不代表真实 OpenAI 模型准确率。
- 在未提供 API Key 和预算前，不运行真实付费请求；真实模型的 Tool Accuracy、Latency、Token 和费用必须单独记录。
- 当前可以表述为“完成可选 OpenAI Provider 接入和离线/在线双模式框架”，不能表述为“真实线上模型评测达到 100%”。

面试表达：
> 我把原来只接受外部 JSON 的 LLM Router 升级为 Provider Gateway。默认是零成本确定性 Planner，Online 模式通过 Responses Structured Outputs 生成 Plan，但仍要经过本地 Tool 白名单和 Schema 二次校验。Provider 只提议 Plan，不能绕过 Evidence、Verifier 和 Policy。针对 Malformed JSON、未知 Tool 和 Timeout，我实现了可审计 fallback；12 条 Offline/Mock-Online 任务 Plan 一致率和故障降级成功率均为 100%，且 Trace 记录 Provider、Model、Latency、Token 和 fallback。真实模型效果会在有明确 API 预算后单独评测，不把 Mock 结果冒充线上效果。

下一步：
- Step 25：小预算真实 Provider A/B（需要显式 API Key 与预算），或优先完成鉴权、外部数据库持久化和并发部署。

工程与安全边界：
- 只有人工 approved Candidate 能创建 Policy，评测通过本身不能自动发布。
- Policy 只能包含受支持的 Router Hook 配置，不能修改源码、数据集、断言、权限或发布门槛。
- 自动回滚依赖确定性阈值，不让 LLM 自己决定是否继续上线。
- 当前 Monitor 和 Policy Repository 适合单进程 Demo；生产化需要外部数据库、分布式锁、鉴权、审计日志和真实可观测平台。

面试表达：
> 在 Feedback 候选通过离线门禁后，我没有直接替换 Router，而是增加了不可变 Policy 版本和基于 session_id SHA-256 的确定性灰度。同一用户多轮请求稳定落在 control 或 rollout，Trace 会记录准确 policy 和 cohort。我用 1,000 个 Session 验证 20% 灰度实际为 18.4%，再注入 rollout 失败样本，通过成功率阈值自动回滚到 v1，且 Router、Planner、Workflow 源码哈希不变。最后用 Docker、CI Smoke、Ruff/Mypy、发布和回滚 Runbook 把实验闭环变成工程交付。

下一步：
- Step 24：接入可选真实 LLM Provider 与离线/在线双模式评测，或先补充鉴权、外部持久化与并发部署能力。

## Step 25：离线受控自进化增强

当前问题：
- Step 22 已能根据人工提交的 Trace Feedback 生成候选，但系统不会主动从离线 Benchmark 中寻找问题，仍依赖人先发现并提交至少 3 条同类反馈。
- 旧 Candidate Proposer 只支持 `wrong_tool -> router_hook`，无法覆盖实体别名缺失和 RAG 排序失败。
- 项目虽然具备 Trace、Evaluation、Policy Rollout 和 Rollback，但“发现失败 -> 归因聚类 -> 生成候选”这一段还没有形成自动化离线链路。
- 当前没有真实 API Key 和预算，不适合把真实 Provider A/B 作为必做前置条件。

为什么要添加：
- 让 Agent 在零外部调用、零 Token 成本下主动扫描可复现失败，而不是等待人工逐条提交 Feedback。
- 将重复 Bad Case 按根因聚类，只有支持数达到阈值才生成候选，避免单样本过拟合。
- 把可优化资产从单一 Router Hook 扩展到 Router Rule、Query Alias 和 Retriever Weight，覆盖规划、实体解析和知识检索三层。
- 在候选进入人工审批前自动运行关联案例和冻结回归集，证明“修复问题”没有变成“转移问题”。
- 保留人工审批、策略版本化、灰度和回滚边界，避免所谓自进化演变成自动改源码或自动上线。

新增/修改内容：
- 新增 `app/evolution/models.py`：定义 MinedFailure、FailureCluster、EvolutionCandidate 和状态模型。
- 新增 `app/evolution/repository.py`：保存一次离线演化周期中的失败、聚类和候选。
- 新增 `app/evolution/miner.py`：运行 Agent 标注集与 Legacy RAG 标注集，自动识别 Router、实体别名和召回排序失败；聚类最小支持数为 2。
- 新增 `app/evolution/candidate.py`：生成 `router_rule`、`query_alias`、`retriever_weights` 三类纯配置候选，校验安全作用域，并在隔离 Session/Trace 中执行 Shadow Evaluation。
- 新增 `app/evolution/service.py`：实现 `scan -> draft -> shadow_evaluating -> pending_review -> approved/rejected` 状态机；自动流程不允许越过 `pending_review`。
- HybridRetriever 新增显式 `rrf_weight` 与 `reranker_weight` 参数；默认值保持 `100/1`，原运行结果不变。
- 新增 `evaluation/evolution_cases.json`：3 条 `prerequisites` Router 盲区和 2 条 `tls toolkit` 实体别名盲区；RAG 失败从现有 30 条标注集自动挖掘。
- 新增 `evaluation/evolution_eval.py` 与 `--suite evolution`，汇总失败数、聚类数、候选类型、修复数、回归数和安全门禁。
- 新增 `/evolution/scan`、`/evolution/state`、候选影子评测、人工审核、激活阻断及 `/evaluation/evolution` API。
- 新增 `tests/test_offline_evolution.py`、设计文档和实验报告，并同步 README、API Contract 与 Project Summary。

实验 Bad Case 与候选：
- Router：`openssl/nginx/tcpdump prerequisites` 均误选 `package_search`，聚为 `router:dependency_analysis:prerequisites`，生成 `prerequisites -> dependency_analysis` Router Rule。
- Entity Alias：`tls toolkit package metadata` 和 `show tls toolkit version` 无法解析软件包，聚为 `alias:openssl:tls toolkit`，生成 `tls toolkit -> openssl` Query Alias。
- Retriever：Legacy RAG 在 `rag-003/007/010/013` 四条中文语义查询上 Recall@3 失败，聚为 `retriever:legacy:recall_at_3`，生成 Hybrid RAG 权重候选。
- 单个失败不会生成候选；当前三个聚类支持数分别为 3、2、4，均达到最小阈值。
- 预编译阶段尝试写 `__pycache__` 被中文工作区权限阻断；实际模块导入、评测和 `python -B` 测试均成功，后续继续用禁缓存模式验收。
- FastAPI 首次重启时，项目目录中的日志重定向文件同样被 ACL 拒绝；改为系统临时目录记录日志后，服务由旧 PID 3504 平滑替换为当前 PID 13400。

添加后的影响：
- 系统现在能主动从离线标注结果中发现重复失败并给出结构化根因，不需要真实 API 或人工逐条反馈。
- 三类候选都在隔离 Shadow Runner 中生效，不修改当前 Router、Alias 表、Retriever 默认模式或 Policy Repository。
- Router/Alias 候选运行 193 条冻结 Agent 回归；Retriever 候选运行 30 条 RAG 回归。
- 通过门禁只能进入 `pending_review`；人工 approve 后仍为 `active=false`，发布必须继续走 Step 23 的版本、灰度、监控和回滚路径。
- `/agent/query` 默认行为和 Step 17 冻结基线保持不变，真实 Provider 仍是可选增强项。

验证结果：
- Offline mined failures：9 条。
- Root-cause clusters：3 个；Candidate types：3 类。
- Linked score：三个候选均从 0% 提升到 100%。
- Fixed Bad Cases：Router 3 条、Query Alias 2 条、Retriever 4 条，共 9 条。
- Router/Alias 冻结回归：各 193 条，Regressed Cases 均为 0。
- Retriever 回归：30 条，核心通过率由 86.67% 提升到 100%，Regressed Cases 为 0。
- Paid API Calls：0；Automatic source changes=false；Automatic activation=false。
- 三个候选状态均为 `pending_review + active=false`。
- Step 25 聚焦测试：20 passed，0 failed。
- 完整自动化测试：100 passed，0 failed；Step 17 冻结基线 193 条全部兼容。
- Ruff：All checks passed；Mypy：9 个本阶段源码模块无类型问题。
- FastAPI HTTP：`/evolution/scan` 返回 9 failures、3 clusters、3 candidates；`/evaluation/evolution` 全门禁通过。
- 单候选 HTTP Shadow Evaluation：status=pending_review、fixed=2、regressions=0、active=false；调用 activate 返回 HTTP 409。

安全边界与诚实表述：
- 本步骤是 `Controlled Self-evolution / Human-in-the-loop Agent Optimization`，不是完全自治、自改代码或 RL 训练。
- Agent 不得自动修改 Python 源码、模拟数据、测试断言、权限或发布门槛。
- 离线 Benchmark 提供可复现证据，但不能代表真实企业数据或真实线上模型效果。
- 未运行真实 Provider A/B 时，不声称真实 OpenAI 模型准确率、Token 成本或线上收益。

面试表达：
> 我在已有 Trace、Evaluation 和 Policy 灰度闭环上增加了离线受控自进化。系统会扫描标注 Benchmark，自动挖掘重复失败并聚类根因，生成 Router Rule、Query Alias 或 Retriever Weight 三类配置候选。候选先修复关联 Bad Case，再分别回放 193 条 Agent 或 30 条 RAG 冻结集；本次自动发现 9 条失败，3 个候选全部把关联成功率从 0% 提升到 100%，总计修复 9 条且零回归。自动流程最高只到 pending_review，不改源码、不改测试、不自动上线，人工审批后仍需通过版本化灰度和回滚链路发布，全程没有调用付费 API。

下一步：
- Step 26 建议优先做外部数据库持久化、鉴权和并发一致性，使 Trace、Feedback、Evolution Candidate 与 Policy 能在多进程部署中可靠共享；真实 Provider A/B 可在 API Key 和预算明确后作为独立实验补做。

## Step 26：外部数据库持久化、API 鉴权和多进程并发一致性

当前问题：
- Session、Trace、Feedback 和 Evolution 状态主要保存在进程内存中，两个 Uvicorn Worker 之间无法共享；同一 Session 被分发到另一 Worker 时会丢失多轮上下文。
- Policy 虽然能写 JSON 文件，但多个进程同时创建版本或切换 rollout 时可能产生版本号冲突、覆盖和读到旧状态。
- Candidate Review、Session 更新和 Feedback 修改没有 revision，两个 Worker 可以基于同一旧对象提交互相覆盖的结果。
- FastAPI 控制面没有身份边界，任何调用方都能尝试执行 Agent、提交反馈、审批候选或回滚策略。
- 本机此前因磁盘不足跳过 Docker/PostgreSQL 实跑，因此需要在不下载镜像的前提下先验证并发语义。

为什么要添加：
- 把单进程 Demo 的状态迁移到可由多个 Worker 共享的控制面，保证多轮 Context、Trace、Feedback 和策略在负载均衡后仍一致。
- 用数据库事务、revision CAS 和分布式租约阻止静默覆盖，给冲突提供明确的 HTTP 409 语义。
- 将读取、执行和审批/发布权限分开，避免普通查询方拥有策略修改权。
- 增加 readiness、脱敏状态和真实 PostgreSQL CI Smoke，为后续并发部署提供可操作的验收路径。
- 保持数据库和鉴权默认关闭，使原 193 条冻结基线、CLI 和本地 Demo 不需要额外配置。

新增/修改内容：
- 新增 `app/storage/database.py`：实现 PostgreSQL/SQLite JSON Record Store、`version` compare-and-swap、事务、TTL Lease、健康检查和凭据脱敏。
- 控制面统一使用 `control_plane_records(namespace, record_id, payload, version, updated_at)` 和 `control_plane_leases(name, owner, expires_at)`。
- SessionRepository、TraceRepository、FeedbackRepository、CandidateRepository 和 EvolutionRepository 增加可选共享 Store；启用数据库时跨进程读取以数据库为准。
- Session、Feedback 和 Evolution Candidate 更新携带本 Worker 最近读取的 revision；陈旧保存抛出 `ConcurrentUpdateError`。
- PolicyRepository 将完整 stable/rollout/version/rollback 状态作为单一事务文档持久化；写入前获取 `policy-state-write` 租约，最长等待 3 秒，并用 revision 二次保护。
- Evolution Scan 增加 120 秒 `evolution-cycle` 租约，阻止多个 Worker 同时清空和重建候选周期。
- 新增 `app/security/api_key.py`：reader/operator/admin 三级 API Key、常量时间比较、请求内 Key 指纹和 fail-closed 配置。
- FastAPI 增加鉴权中间件：缺 Key/非法 Key 为 401，角色不足为 403，未配置 Key 为 503，CAS/Lease 冲突为 409。
- 新增 `/ready`、`/auth/status`、`/storage/status` 和 `/evaluation/control-plane`；`/health` 继续表示进程存活。
- `requirements-runtime.txt` 增加 `psycopg[binary]`，PostgreSQL 驱动仅在配置 PostgreSQL URL 时加载。
- Docker Compose 增加 PostgreSQL 16、持久卷、健康检查、数据库 URL、API Key 环境变量，并将 Uvicorn 配置为 2 Workers。
- GitHub Actions 增加 `postgres-integration` Job，通过真实 PostgreSQL Service 运行 `evaluation/postgres_smoke.py`。
- 新增 `tests/test_control_plane.py`、`evaluation/control_plane_eval.py`、控制面设计文档和评测报告，并同步 README、API Contract、Project Summary 与 CI Smoke。

实验问题与修复：
- 第一版 Policy Lease 正确阻止双写，但第二个正常并发请求立即失败；对短 Policy 事务增加最多 3 秒有界等待，使两个请求串行生成 v2/v3，同时保留超时冲突。
- Starlette TestClient 在当前捆绑环境要求未安装的 `httpx2` 兼容包，导致 HTTP 单测收集失败；改为单测鉴权决策矩阵，并计划用真实 Uvicorn + Invoke-WebRequest 验收 401/403/200。
- Session 最初未纳入改造，审计发现多 Worker 会导致多轮实体继承丢失；补充 Session 数据库 Repository 和跨 Store 上下文测试。
- 本机不拉取 PostgreSQL/Docker 镜像，防止再次触发磁盘空间问题；使用两个独立 SQLite WAL Store 验证同一 SQL Record/CAS/Lease 语义，真实 PostgreSQL 路径交由 CI Service Smoke。

添加后的影响：
- 配置数据库后，不同 Worker 可共享 Session、Trace、Feedback、Candidate、Evolution 和 Policy，不再依赖单个进程命中。
- 同一记录的两个并发更新只有一个能基于原 revision 成功，另一个得到明确冲突，不会悄悄覆盖审批或上下文。
- Policy 版本创建、rollout、promote 和 rollback 在数据库租约内串行执行，跨 Repository 能立即看到最新 stable/rollout。
- API 权限分为 reader、operator、admin；管理员 Key 可读，reader Key 不能执行 Agent 或修改策略。
- 默认未设置 `SOFTWARE_AGENT_DATABASE_URL` 和 `SOFTWARE_AGENT_AUTH_ENABLED` 时，项目继续使用原本地模式，现有调用方不受影响。

验证结果：
- 控制面专项测试：12 passed，0 failed。
- Context + Control-plane 联合测试：20 passed，0 failed。
- 两个独立 Store 实例共享 Record/Session/Trace/Feedback/Policy：通过。
- Stale revision rejection、Lease exclusive/recovery、Evolution double-review rejection：通过。
- 并发 Policy 创建：唯一版本 `[2, 3]`，跨 Repository policy count 均为 3。
- Control-plane Evaluation：13/13 门禁通过，Bad Case 0，Paid API Calls 0。
- CI Smoke：Core、Policy、Provider、Control-plane 全部通过。
- 完整自动化测试：112 passed，0 failed；Step 17 冻结基线 193 条全部兼容。
- Ruff：All checks passed；Mypy：10 个控制面边界模块无类型问题。
- 双 Worker HTTP 服务：Parent PID 22116，Worker PID 23096/2552，均完成启动。
- HTTP 鉴权：无 Key 读取 `/tools`=401；reader 读取=200、执行 Agent=403；operator 执行 Agent=200、Policy 回滚=403；admin 能进入 Policy 业务处理并对不存在版本返回 409。
- HTTP 共享状态：首轮 Trace 跨请求读取=200；同 Session 第二轮“它的依赖”解析为 `openssl的依赖`，turn_count=2、packages=openssl。
- HTTP Storage/Ready：backend=sqlite、healthy=true、ready=ready；API Key 与数据库 credentials exposed 均为 false。
- 共享数据库实际记录：policy_state=1、session=1、trace=2。
- `/evaluation/control-plane`：13/13 门禁通过，Bad Case 0，Paid API Calls 0。

边界与诚实表述：
- 本机验证的是 SQLite WAL 多连接并发语义；PostgreSQL Adapter、Compose 和 CI Job 已实现，但尚未在本机真实构建或启动 PostgreSQL 容器。
- 在 CI Job 或其他 PostgreSQL 环境实际执行前，应表述为“PostgreSQL-ready，共享状态与并发语义已在 SQLite WAL 验证”，不能表述为“本地 PostgreSQL 已部署”。
- 当前 API Key 适合 Demo/内部服务边界；生产环境仍应接 Secret Manager、Key 轮换、审计日志或 OAuth/OIDC。
- 当前启动时自动建表，不等同于正式 Schema Migration；生产化还需要迁移、连接池、备份和数据保留策略。

面试表达：
> 我把原来分散在进程内存和单机 JSON 的 Agent 控制面抽象成共享数据库。Session、Trace、Feedback、Evolution Candidate 和 Policy 都可以跨 Worker 读取；每条可变记录带 revision，陈旧写入通过 CAS 拒绝，Policy 变更再用带 TTL 的数据库租约串行化。我用两个独立 Store/Repository 实例模拟多 Worker，并发创建策略稳定得到 v2/v3，旧 Candidate Review 无法覆盖新结果。API 侧增加 reader、operator、admin 三级 Key，区分查询、执行和审批发布权限，密钥不进入 Trace 或响应。本地 13 项控制面门禁全部通过，部署侧配置 PostgreSQL、两 Uvicorn Worker 和真实数据库 CI Smoke，同时保留默认离线兼容模式。

下一步：
- Step 27 建议进行真实 PostgreSQL/多 Worker 压测与故障注入；磁盘条件允许前，也可先实现数据库 Migration、Key Rotation/Audit 和 Trace Retention Job。

## Step 27：多 Worker 压测、故障注入、Schema Migration、Key Rotation/Audit 与 Trace Retention

当前问题：
- Step 26 使用启动时 `CREATE TABLE IF NOT EXISTS`，没有版本历史和脚本校验；多个 Worker 同时启动时也缺少正式的迁移互斥语义。
- API Key 只能通过环境变量静态配置，轮换必须重启所有 Worker，旧 Key 无 grace window，也不能统一撤销。
- 控制面缺少结构化审计，无法回答“谁在何时调用、审批、轮换或清理了什么”，同时又要避免把原始 Key 和用户问题写入日志。
- Trace、Session、Feedback、Evolution 和 Audit 没有生命周期策略，长期运行会持续增长。
- Step 26 只验证了两个独立 Store 的一致性，没有形成可重复的 HTTP 并发指标、Worker 崩溃恢复和数据库停机恢复门禁。
- 本机磁盘不足，不能重新下载 PostgreSQL/Docker 镜像，因此必须区分本机 SQLite WAL 实测和真实 PostgreSQL CI 待执行结果。

为什么要添加：
- Schema Migration 让数据库结构变化可追溯、可重复执行、可校验，避免应用代码与数据库版本不一致时继续带病运行。
- Key Rotation 让密钥生命周期不再依赖进程重启；数据库共享 Registry 保证多个 Worker 同时接受新 Key、拒绝被撤销 Key。
- Audit 为鉴权拒绝、控制面修改和运维操作提供可检索证据，同时通过字段白名单保护 Key、请求正文和 Agent 上下文。
- Retention 将隐私与容量治理变成显式策略，并用 dry-run、分批上限和外部调度边界降低误删风险。
- 多 Worker 压测与故障注入验证的不只是“正常请求能返回”，而是并发、进程退出、事务异常和数据库不可用时的系统语义。
- 把真实 PostgreSQL 场景固化进 CI，可以在本机资源受限时继续完成工程实现，但不会把未执行的 CI 配置冒充实测成绩。

新增/修改内容：
- 新增 `app/storage/migrations.py`：定义 v1 Record/Lease、v2 Key Registry/Audit、v3 Retention Index 三个迁移版本。
- 新增 `control_plane_schema_migrations`，记录 version、name、SHA-256 checksum 和 applied_at；已应用脚本发生漂移时抛出 `MigrationChecksumError` 并阻止启动。
- PostgreSQL Migration 在同一事务内获取 `pg_advisory_xact_lock`，SQLite 使用 `BEGIN IMMEDIATE`，防止多 Worker 重复并发迁移。
- `ControlPlaneStore.status()` 升级为 `control-plane-v3`，返回 current/latest/applied_now/up_to_date；新增按 namespace、时间和 batch limit 的可移植清理接口。
- 新增 `app/security/key_registry.py`：数据库托管 reader/operator/admin Key，原始 Key 只返回一次，数据库只保存 `SHA-256(pepper:key)`。
- Key 轮换支持 TTL 和 grace window；角色一旦由 Registry 接管，该角色的旧环境变量 Key 不再作为旁路继续生效。
- 新增 Key 列表、轮换和撤销 API；禁止撤销最后一个可用的数据库托管 admin Key。
- 新增 `app/security/audit.py`：记录 authentication、authorization、api_request、api_key_rotation、api_key_revocation 和 retention 事件。
- Audit 只记录 12 位 Key fingerprint、角色、资源、动作、结果、状态码和时间，不记录原始 Key、Secret Hash、请求正文、Query 或 Agent Context。
- 新增 `app/maintenance/retention.py` 与 `retention_job.py`：Session 7 天、Trace 30 天、Feedback/Candidate 90 天、Evolution 临时数据 30 天、Audit 180 天；均可通过环境变量覆盖。
- Retention 默认 dry-run，单次最多 10,000 条；Policy State 不参与按时间自动删除，生产调度继续由 cron/Kubernetes CronJob/托管调度器负责。
- 新增 `/auth/keys`、`/auth/keys/rotate`、`/auth/keys/{key_id}/revoke`、`/audit/events`、`/maintenance/retention/policy` 和 `/maintenance/retention/run`。
- HTTP 响应增加 `X-Agent-Worker-Pid`，仅用于证明压测实际分布到多个 Worker。
- 新增 `evaluation/step27_load.py`：统计成功率、5xx、p50/p95 延迟和 Worker 数量。
- 新增 `evaluation/step27_fault_eval.py`：注入 stale CAS、Lease contention、Lease expiry 和事务异常，验证拒绝、恢复与 rollback。
- PostgreSQL CI Job 增加两 Worker API、100 请求并发压测、强制杀死一个 Worker、40 请求恢复压测、PostgreSQL stop/start 以及 `/ready` 503 -> 200 验收。
- 新增 Step 27 专项测试、生产运维文档和评测报告，并同步 README、API Contract、Project Summary 和面试材料。

实验问题与修复：
- Ruff 全仓默认规则暴露出较多历史格式与现代化提示，这些不是本步骤引入；本次对新增/修改文件单独运行完整 Ruff，并继续使用 CI 既有的 E9/F63/F7/F82 全仓门禁，避免无关大规模格式化。
- `rg.exe` 在当前 Codex WindowsApps 路径被系统拒绝执行，文件检索临时切换为 PowerShell `Select-String`，没有修改项目行为。
- 本地 Worker 终止后 `Get-CimInstance` 因系统权限无法读取进程树，但恢复压测通过响应 Header 观察到原 PID 20404 被新 PID 23292 替代，因此仍形成了可验证的进程恢复证据。
- 本机未启动 PostgreSQL。真实数据库 stop/start 只写入隔离的 GitHub Actions Service Job，避免当前磁盘问题再次影响开发环境。

添加后的影响：
- 数据库 Schema 从隐式建表升级为 v1-v3 可追溯迁移，重复启动不重复应用，脚本漂移会 fail-closed。
- Key 无需重启 Worker 即可轮换；本地两个 Worker 共享新 Key 状态，旧 operator Key 在 grace=0 时立即返回 401，新 Key 返回 200。
- 受保护请求和高风险控制面操作可通过 Audit 查询，Registry/Audit 响应不包含原始 Key。
- Trace 等状态具有明确保留期限、dry-run 预览和分批删除能力，降低长期容量与隐私风险。
- 真实并发与进程恢复有统一机器可读报告，不再只依赖手工“请求成功”的演示。
- `/ready` 继续代表数据库依赖健康；CI 将验证 PostgreSQL 停机时 503、恢复后 200，而 `/health` 仍只代表进程存活。

验证结果：
- Step 27 专项与原控制面联合测试：19 passed，0 failed；Step 26 legacy schema 无损接管通过。
- 完整自动化测试：119 passed，0 failed；Step 17 冻结基线 193 条全部兼容。
- Migration：首次应用 `[1,2,3]`，第二次应用 `[]`；checksum drift 拒绝通过。
- 本地事务故障注入：6/6 gates passed，包括 stale CAS、Lease contention、Lease expiry recovery、transaction rollback、schema latest 和 database health。
- 首轮本地双 Worker HTTP：60/60 成功，success rate 100%，server errors 0；观察到 PID 20404/22160。
- 首轮延迟：p50 358.22 ms，p95 1374.40 ms，总耗时 2.97 秒，并发 12。
- 强制终止 PID 20404 后，Uvicorn supervisor 启动 PID 23292。
- 恢复轮：40/40 成功，server errors 0；观察到 PID 22160/23292；p50 218.07 ms，p95 847.12 ms。
- HTTP Key Rotation：returned_once=true；旧 operator Key=401，新 Key=200；Registry raw_key_in_list=false、secret_exposed=false。
- Audit 查询返回结构化事件；原始 Key 和 Secret Hash 未出现在列表。
- Retention 单测同时验证 dry-run、batch_limit=1、真实删除和 Audit 清理，新记录不受影响。
- 本次新增/修改文件完整 Ruff：All checks passed；CI 同款全仓 Ruff：All checks passed；Mypy：37 个控制面源码模块无类型问题。
- 真实 PostgreSQL Migration/Load/Worker Kill/Database Outage：CI 已实现，当前本机未执行，结果待 GitHub Actions 或其他 PostgreSQL 环境确认。

边界与诚实表述：
- 本机压测使用 SQLite WAL 共享数据库，Worker 故障是真实终止并恢复；不能把本机延迟数字称为 PostgreSQL 性能。
- PostgreSQL 16 Service、100 请求压测和数据库 stop/start 是可执行 CI 门禁，但在工作流实际成功前只能表述为“已配置/待执行”。
- 数据库 Key Registry 适合项目级内部 API；真正生产系统还应由 Secret Manager 管理 pepper/bootstrap Key，并考虑 OAuth/OIDC、连接池和集中审计平台。
- Audit 是应用级 best-effort 记录，数据库完全不可用时不能保证写入；高保证审计需要独立日志管道。
- Retention 能力已实现，生产环境仍需外部调度、备份、恢复点目标和删除合规审批。

面试表达：
> 我在共享 Agent 控制面上补了一层可运维能力。数据库从启动时建表升级为带 checksum 的 v1-v3 Migration，PostgreSQL 用 advisory transaction lock 保证多 Worker 只迁移一次；脚本漂移会直接阻止启动。API Key 改为数据库共享的哈希 Registry，支持 grace window、TTL、撤销和全 Worker 即时生效，同时用字段白名单 Audit 记录鉴权与高风险操作。Trace、Session、Feedback 和 Evolution 增加 dry-run、分批上限和可配置保留周期。本地 60 次双 Worker 并发请求零 5xx，杀掉一个 Worker 后新进程补位，恢复轮 40 次仍全部成功；事务故障门禁 6/6 通过。真实 PostgreSQL 停机和恢复已固化到 CI，但由于本机磁盘限制尚未执行，所以我会明确区分 SQLite WAL 本机结果和 PostgreSQL 待验证结果。

下一步：
- Step 28 建议优先执行一次 GitHub Actions PostgreSQL Job 并保留报告；若仍无法提供 CI 环境，则可继续实现连接池、Prometheus 指标、备份恢复演练和独立审计日志出口。真实 Provider A/B 仍可在 API Key 与预算明确后单独补做。

## Step 28：连接池、Prometheus、备份恢复与独立审计留证

当前问题：
- Step 27 首选动作是执行一次真实 PostgreSQL GitHub Actions Job，但当前 `Software-Agent` 目录没有 `.git`、GitHub remote、`gh` 可执行文件或已认证 GitHub 会话，无法触发或下载真实 Actions Run。
- PostgreSQL Adapter 每个事务都新建连接，多 Worker 压力下会增加连接握手开销，也缺少连接数和等待请求的可见性。
- 项目只有状态和评测接口，没有 Prometheus 标准指标，无法持续观察请求量、延迟、鉴权拒绝、Retention 和数据库健康。
- Step 27 Audit 以数据库为主，数据库不可用时应用级审计插入也会失败；缺少可由日志采集器独立接收的出口。
- 已有 Retention 负责删除，却没有带完整性校验的备份和恢复演练，无法证明“删前可备、损坏可拒绝、删除后可恢复”。
- GitHub Actions 即使未来运行，原工作流只在控制台输出结果，没有统一 artifact，失败后证据不容易集中保存。

为什么要添加：
- 每 Worker 独立连接池可以复用 PostgreSQL 连接并设置明确上限，避免 Worker 数量与每请求建连共同放大数据库压力。
- Prometheus 指标把一次性测试升级为持续可观测能力，并通过受限 Label 防止 Query、Trace ID 等高基数字段和隐私内容进入时序库。
- 独立 Audit JSONL 让数据库审计失败时仍有第二条 best-effort 出口，可由 Fluent Bit、Vector 或平台日志代理发送到独立存储。
- Backup Manifest、Checksum 和 Restore Lease 让恢复过程有确定性门禁，避免损坏快照或并发恢复直接覆盖控制面。
- CI Artifact 能保留 Smoke、Fault、Load、Metrics、Audit、Outage 和 Restore 原始报告，为面试和故障复盘提供可下载证据。
- 在缺少 GitHub 和真实 PostgreSQL 环境时，先完成本地确定性实现与演练，不虚构 CI 成绩，也不阻塞工程推进。

新增/修改内容：
- `ControlPlaneStore` 增加 PostgreSQL `psycopg_pool.ConnectionPool`；每个 Uvicorn Worker 独立创建和关闭 Pool，SQLite 固定不启用。
- 新增 `SOFTWARE_AGENT_DB_POOL_ENABLED/MIN_SIZE/MAX_SIZE`；min > max 或非法环境值直接拒绝启动。
- `/storage/status` 增加脱敏 pool enabled/min/max/size/available/waiting；数据库 URL 和凭据仍不返回。
- Migration 或启动失败时主动关闭已创建 Pool；FastAPI 使用 lifespan 在 Worker shutdown 时释放连接。
- 新增 `app/observability/metrics.py` 与 `GET /metrics`，输出 Prometheus 0.0.4 文本。
- 指标覆盖 HTTP request counter、固定 bucket latency histogram、auth denial、audit sink failure、retention deletion、storage health 和 pool gauges。
- 生产依赖增加官方 `prometheus-client`，通过 `PROMETHEUS_MULTIPROC_DIR` 聚合多 Worker；本机缺少该包时使用零依赖固定十 bucket fallback。
- Metrics Label 只允许 method、route template、status、reason 和 namespace；鉴权在路由前失败时统一标记 `/unmatched-protected-route`，不泄露 URL 中的真实 ID。
- 新增 `app/observability/audit_sink.py`：支持独立 JSONL 文件或 stdout，每条事件标记 `audit-event-v1`。
- Audit Sink 禁止 `api_key/secret/secret_hash/request_body/query` 字段；数据库写入异常时通过 finally 仍尝试独立出口，出口失败只增加指标、不影响用户请求。
- 新增 `app/storage/backup.py` 和 `backup_cli.py`：逻辑备份 `control_plane_records`，保存 format、created_at、record_count 和 SHA-256。
- Restore 在写入前校验格式、记录数和 checksum，并获取 300 秒 `control-plane-restore` Lease；支持按 namespace 备份和 clear-existing 恢复。
- 普通逻辑备份明确排除 API Key Registry 和 Audit，避免密钥哈希和安全日志进入普通项目备份；完整灾备仍需数据库级加密备份。
- 新增 `evaluation/backup_restore_drill.py`：写入两条记录、备份、验证、删除、恢复、逐条比对并清理。
- PostgreSQL CI 增加 Pool/Metrics/Audit/Backup 门禁，所有输出写入 `artifacts/step28/`，最后通过 `actions/upload-artifact@v4` 且 `if: always()` 上传。
- Artifact 包含 postgres-smoke、fault-injection、backup-restore、两轮 load、storage-status、prometheus-metrics、database-outage 和 audit JSONL。
- Docker Compose、`.env.example`、`.gitignore`、README、API Contract、Project Summary、Interview Talking Points 和 Step 28 运维文档同步更新。

实验问题与修复：
- 当前捆绑 Python 没有 `psycopg_pool` 和 `prometheus_client`，因此无法本机实测 PostgreSQL Pool 或官方 multiprocess collector；运行依赖已更新，真实路径交给 PostgreSQL CI。
- 第一版本地 Histogram 保存每个 latency 样本，长期运行会自行增长；改为 count、sum 和 10 个固定 bucket，内存占用不随请求数增加。
- 第一版自研 Prometheus 仅能看到响应 `/metrics` 的单 Worker；改为部署时使用官方 `prometheus-client` multiprocess 目录聚合，零依赖实现仅作为本地 fallback。
- 鉴权失败发生在 FastAPI 路由匹配前，直接使用原 URL 会造成 ID 高基数和潜在泄露；失败请求统一使用固定 route label。
- 新依赖从 `psycopg[binary]` 升级为 `psycopg[binary,pool]` 后，Step 26 的精确字符串测试失败；更新契约断言，并同时要求 Prometheus、Artifact、Backup 和 Audit 配置。
- FastAPI 完整回归最初产生 `on_event` deprecated warning；将 Pool shutdown 改为 lifespan handler 后消除新增弃用用法。
- GitHub Actions 无法执行的原因已实际检查确认，不是网络猜测：项目不是 Git 仓库、没有 remote，系统无 `gh` 命令输出。

添加后的影响：
- PostgreSQL 请求不再按事务反复建立连接，每个 Worker 的连接上限和等待状态可以配置、查询和监控。
- `/metrics` 可直接接 Prometheus；多 Worker 部署使用官方 multiprocess collector，本地无依赖模式保持测试与 Demo 可运行。
- Query、Session/Trace ID、API Key 和请求正文不会作为 Metrics Label，降低隐私泄露和时序高基数风险。
- Audit 从单一数据库表扩展为数据库加独立 JSONL 出口；数据库异常与日志出口异常分别可观测。
- 控制面可以生成可校验逻辑快照，快照损坏会在写数据库前被拒绝，恢复过程受数据库 Lease 保护。
- GitHub Actions 一旦具备仓库环境，会自动留下统一证据包，即使后续 Outage Gate 失败也保留前置报告。
- SQLite、本地离线 Agent 和 Step 17 冻结行为不启用 Pool，也不要求安装真实 PostgreSQL。

验证结果：
- Step 26-28 聚焦测试：24 passed，0 failed。
- 完整自动化测试：124 passed，0 failed；Step 17 冻结基线 193 条全部兼容。
- Backup Restore Drill：checksum verified=true，删除 2 条、恢复 2 条、逐条值一致，passed=true。
- Corrupted Backup：修改 payload 后 checksum mismatch，恢复前拒绝。
- Backup Security：includes_api_key_registry=false，includes_audit=false。
- 独立 Audit：安全事件成功写入 JSONL；注入数据库 transaction failure 后 JSONL 仍成功保存 failed event。
- Audit Filter：包含 `api_key` 的事件被拒绝，文件中无敏感值。
- HTTP `/agent/query`=200；`/metrics`=200，包含 request counter、latency histogram 和 storage healthy gauge。
- HTTP Metrics 泄露检查：测试 Query 不在 Metrics；operator Key 不在 Audit；Audit 两条事件均含 `audit-event-v1`。
- SQLite Pool status：enabled=false、credentials_exposed=false，保持本地兼容。
- 新增/修改模块完整 Ruff：All checks passed；CI 同款全仓 Ruff：All checks passed。
- Mypy：42 个控制面与可观测性源码模块无类型问题。
- Paid API Calls：0；真实 Provider A/B 未执行。
- GitHub Actions/PostgreSQL Pool/Prometheus multiprocess：已实现、待仓库和 CI 环境执行，不能记为真实通过。

边界与诚实表述：
- 本机只验证 SQLite、Prometheus fallback、JSONL Audit 和逻辑备份恢复；没有真实 PostgreSQL Pool 性能数据。
- CI Artifact 工作流是代码实现，不是已存在的 GitHub Run；当前不能提供 Run URL 或 Artifact 下载链接。
- 逻辑备份只覆盖 `control_plane_records`，不替代 PostgreSQL `pg_dump`、WAL/PITR、加密、异地复制和恢复点目标。
- JSONL 是独立审计出口，不是不可篡改审计仓；生产应接集中日志/SIEM，并设置独立权限与保留周期。
- Prometheus 指标已限制 Label，但生产还需告警规则、Dashboard、采集鉴权和容量预算。
- API Key 与预算未明确前，继续不运行真实 Provider A/B，也不声称真实模型准确率、Token 或成本收益。

面试表达：
> Step 28 我优先检查能否直接跑 PostgreSQL GitHub Actions，但当前项目没有 Git 仓库、remote 和 gh 凭据，所以我没有伪造 CI 结果，而是先补齐可执行的生产能力。每个 Uvicorn Worker 使用有上限的 psycopg Pool，Prometheus 通过官方 multiprocess 模式聚合请求、延迟、鉴权、Retention、数据库和 Pool 指标，并禁止 Query 和 Trace ID 进入 Label。Audit 在数据库之外还有严格字段过滤的 JSONL 出口，即使数据库写失败也会尝试保留事件。我还实现了带 SHA-256 和恢复租约的逻辑备份，实际完成 2 条记录的备份、删除和恢复，并验证损坏快照会被拒绝。CI 已配置把 PostgreSQL Smoke、故障、压测、Metrics、Audit、Outage 和 Restore 报告统一上传为 artifact，但在真实 Run 前我只表述为已实现待执行。

下一步：
- Step 29 首选把当前目录初始化为 Git 仓库并推送到用户指定的 GitHub private/public repository，随后实际运行 PostgreSQL Job、下载 `software-agent-step28-postgres-evidence` 并回填报告；这一步涉及远程仓库和账号授权，必须由用户明确仓库地址与可用认证方式。
- 若暂不连接 GitHub，可继续补充 PostgreSQL `pg_dump/pg_restore` 加密备份脚本、Prometheus alert rules/Grafana dashboard、审计日志签名链和容量基准。真实 Provider A/B 继续作为独立预算实验。

## Step 28.1：GitHub 上传与真实 PostgreSQL CI 留证

当前问题：
- Step 28 工程实现完成，但当时没有 Git 仓库、remote 和 CI 身份，只能把 PostgreSQL Pool、Prometheus multiprocess 和数据库停机恢复记录为“已配置待执行”。
- 公开上传可能暴露学生邮箱、运行日志、Trajectory、Audit、Backup 或 Token，需要先处理提交身份和忽略规则。

为什么要添加：
- 将“CI 配置存在”升级为“真实 GitHub-hosted Runner 已执行并可下载证据”。
- 用真实 PostgreSQL 16 验证 Migration、连接池、跨 Worker 状态、故障恢复和备份恢复，消除 SQLite 与 PostgreSQL 方言差异的不确定性。
- 保存原始 Artifact，让面试中的性能与故障数据可以追溯到具体 commit、run 和文件。

新增/操作内容：
- 对待提交文件执行 Token、PAT、私钥、密码和大文件扫描，未发现敏感凭据或超限文件。
- `.gitignore` 排除 Trajectory、Audit、Backup、Artifact 和测试日志；新增 `.gitattributes` 固定 LF。
- 初始化 `main` 仓库，提交作者改为 `Isoldelu <Isoldelu@users.noreply.github.com>`，避免公开学生邮箱。
- 通过 Git Credential Manager 浏览器 OAuth 登录 GitHub，不接收或输出用户密码、验证码和 Token。
- 创建私有仓库 `Isoldelu/software-engineering-agent`，推送 commit `53405f1f65cdb5963f2515bde3c477e94a202539`。
- GitHub Actions 自动触发 Run `32738626804`；test-and-evaluate、docker-build、postgres-integration 三个 Job 全部 success。
- 下载并核验 Artifact `software-agent-step28-postgres-evidence`，Artifact ID `9524236638`，到期时间 2026-11-22。

真实 PostgreSQL 验证结果：
- PostgreSQL backend、共享 Record、Exclusive Lease：全部通过。
- Schema：`control-plane-v3`，current=latest=3，migration_up_to_date=true，credentials_exposed=false。
- Fault Injection：stale CAS、Lease contention、Lease expiry、transaction rollback、schema、health 共 6/6 passed。
- 初始多 Worker Load：100/100 成功，success_rate=100%，server_errors=0，并发 16。
- 初始延迟：p50=145.74 ms，p95=456.14 ms，总耗时 1.23 秒；观察到 Worker PID 2583/2584。
- 强制终止一个 Worker 后，新 PID 2667 补位；恢复 Load 40/40 成功，server_errors=0。
- 恢复延迟：p50=85.99 ms，p95=279.13 ms，总耗时 0.61 秒；观察到 PID 2584/2667。
- PostgreSQL Pool：enabled=true，min=1，max=8，pool_size=8，available=8，requests_waiting=0。
- Backup Restore：checksum 通过，删除 2 条、恢复 2 条，API Key Registry/Audit 未进入普通备份。
- Database Outage：PostgreSQL 停机时 `/ready=503`，重启恢复后 `/ready=200`。
- Audit Artifact：142 行全部带 `audit-event-v1`，未发现 operator Key 或原始 Query。
- Prometheus Artifact：包含 HTTP Request 和 Pool 指标，未发现原始 Query。

添加后的影响：
- Step 28 从“PostgreSQL-ready”正式升级为“真实 PostgreSQL CI 全门禁通过并有 Artifact 留证”。
- 项目已托管到私有 GitHub 仓库，远程 `main` 与本地 commit 一致。
- 后续修改可走正常 Git 提交、push 和 Actions 回归，不再依赖本机 Docker 空间。

边界与诚实表述：
- 这些延迟来自 GitHub-hosted Runner 上 100/40 请求的功能压测，不代表生产容量上限或 SLA。
- 仓库当前为 private；改为 public 前仍应再次检查实验记录、个人信息和展示范围。
- Artifact 有到期时间，长期留证应保存摘要、Release Asset 或外部归档。

面试表达：
> 我把 Step 28 的 PostgreSQL 门禁真正放到 GitHub Actions 上跑通并保存 Artifact。真实 PostgreSQL 16 环境中，v3 Migration、共享状态、Lease 和 6 类事务故障全部通过；两个 Uvicorn Worker 完成 100 次并发请求，成功率 100%、零 5xx，杀掉一个 Worker 后新进程补位，恢复轮 40 次仍全部成功。数据库停机时 readiness 正确降为 503，重启后恢复 200；Pool、Prometheus、Audit 和 2 条记录的备份恢复也通过。所有结果绑定到 commit 53405f1 和 Run 32738626804，我把 CI 延迟明确表述为功能门禁结果，不冒充生产 SLA。

下一步：
- Step 29 可进行 GitHub 工程展示整理：仓库公开前审计、Issue/PR 模板、Release v1.0、架构图和演示素材；或者进入小预算真实 Provider A/B。

## Step 29：开源工程整理与 v1.0.0 可复现发布

当前问题：
- Step 28.1 已有私有 GitHub 仓库和真实 PostgreSQL 证据，但首页信息层级、开源治理文件和版本发布入口仍不完整，更像实验仓库而不是可交付项目。
- CI 证据只有会过期的 Actions Artifact，没有稳定的 Release 摘要、版本边界和安全披露方式。
- 首次发布候选提交 `44614a3` 的 Run `32740795037` 出现一次 Feedback Loop 测试失败：候选在人工审核前被延迟门禁误判为 `rejected`。
- 失败暴露出原门禁使用单轮平均延迟和纯 15% 相对阈值；约 1 ms 的本地操作在共享 CI 上容易被调度尖峰放大。

为什么要添加：
- README 第一屏需要让面试官在几十秒内看到项目定位、核心能力、真实证据和最短运行方式。
- License、Security、Contributing、Issue/PR 模板、CODEOWNERS 和 Dependabot 是公开协作、漏洞披露和依赖维护的基础契约。
- Changelog、Release Notes 和机器可读 Evidence 将功能、边界、Commit、CI Run 与 Artifact 绑定，避免演示数据无法追溯。
- 修复真实 RC 失败比简单重跑更重要，可以证明项目具备“Trace/评测发现问题 -> 定位门禁噪声 -> 策略修正 -> CI 回归”的优化闭环。

新增/修改内容：
- README 第一屏新增 At A Glance、Verified Evidence 和 Try It，集中展示 Agent、RAG、Evidence、Feedback、部署与运维能力。
- 新增 MIT `LICENSE`、`SECURITY.md`、`CONTRIBUTING.md`、`CHANGELOG.md` 和 `CODEOWNERS`。
- 新增 Bug/Feature Issue 模板、PR 模板、Issue 配置和 Dependabot 周期更新配置。
- 新增 `docs/public-release-audit.md`，记录提交历史、凭据、路径、数据、运行产物和大文件的公开前检查项。
- 新增 `release/RELEASE_NOTES_v1.0.0.md` 与 `release/v1.0.0-evidence.json`，保存可读发布说明和机器可读证据。
- 新增 Step 29 发布契约测试，防止治理文件、忽略边界或证据字段在后续修改中丢失。
- Candidate 延迟统计由 mean 改为 median，降低单个调度尖峰影响；保留 15% 相对上限，并增加 0.5 ms 绝对噪声容差。
- 延迟评测新增 `added_latency_ms`、统计方法、噪声下限和比例上限字段，保证门禁判断可解释。
- 新增门禁测试：小于 0.5 ms 的 CI 波动放行，1 ms 级明显退化仍拒绝。

实验问题与修复：
- 首次 RC 本地 127 项测试通过，但 GitHub Actions 的 Feedback 候选未进入 `pending_review`；沿状态流转定位到 `added_latency_within_15_percent` 门禁。
- 没有用重跑掩盖问题，而是将微基准改为抗离群值统计和双阈值判断，同时保持历史字段兼容。
- 本地 Codex 捆绑 Python 在 pytest 输出 100% 后出现第三方会话清理挂起；断言结果完整通过，进程被主动清理，正式发布结果以纯净 GitHub Runner 为准。
- 修复提交 `01a4737` 触发 Run `32742656550`，test-and-evaluate、docker-build、postgres-integration 三个 Job 全部 success。

添加后的影响：
- 项目现在具备从 README 浏览、问题提交、代码贡献、安全披露到版本发布的完整开源协作入口。
- 评测门禁不再因亚毫秒级环境噪声错误拒绝候选，同时仍能阻止超过绝对容差且超过 15% 的真实性能退化。
- v1.0.0 的测试、PostgreSQL、故障恢复、隐私检查和性能数据都能追溯到固定 Commit、Run 和 Artifact。
- 仓库继续保持 private；所有公开准备完成，但公开动作仍由仓库所有者显式决定。

验证结果：
- GitHub Actions Run `32742656550`：三个 Job 全部 success。
- 自动化测试：128 passed；Step 17 冻结评测：193/193 compatible。
- Ruff：All checks passed；Mypy：36 个发布相关控制面模块无类型问题。
- PostgreSQL Fault Injection：6/6 passed；初始 Load 100/100、恢复 Load 40/40，均为 0 server errors。
- Worker 2574 被替换为 2679；恢复后仍观察到两个 Worker。
- Database Outage：`/ready` 503 -> 200；Backup Restore：2/2。
- Artifact `9525774190` 共 142 行 Audit，未发现测试 API Key 或原始 Query；Prometheus 同样未发现原始 Query。
- Paid API Calls：0；真实 Provider A/B 仍未执行。

边界与诚实表述：
- GitHub CI 延迟是功能压测，不是生产 SLA；本次 p50/p95 会随 Runner 资源波动。
- 0.5 ms 是微基准噪声下限，只用于离线候选门禁；生产策略还应使用多轮预热、分位数和独立基准环境。
- 所有软件资产数据仍为公开风格模拟数据，不包含或依赖企业内部数据。
- 仓库当前是 private；创建 Release 不等同于公开仓库。
- Offline Proxy 不是实际 LLM 效果或成本；真实 Provider A/B 仍需要明确 API Key 和预算。

面试表达：
> 我把实验仓库整理成了可复现的 v1.0 发布，补齐 License、Security、贡献规范、Issue/PR 模板、Changelog 和机器可读证据。发布候选在 GitHub Actions 上曾因微秒级延迟波动被错误拒绝，我沿 Feedback 候选状态定位到纯 15% 相对门禁，改为中位数加 0.5 ms 噪声下限，同时保留真实性能退化拦截。修复后的 Run 32742656550 中 128 项测试、Docker 和真实 PostgreSQL 三个 Job 全绿，100/40 请求均零 5xx，6/6 故障门禁和数据库恢复通过。这个过程也形成了一次真实的 Bad Case 优化闭环。

下一步：
- 在保持 private 的前提下创建 `v1.0.0` Git Tag 和 GitHub Release，并上传固定 Release Asset；确认展示范围后再由仓库所有者决定是否公开。
- 后续可做 Step 30 小预算真实 Provider A/B，或优先制作架构图、API 演示录屏和面试讲解材料。

## Step 29.1：v1.0.0 Tag、Release 与固定证据资产

当前问题：
- Actions Artifact 会在 2026-11-22 到期，只依赖临时 Artifact 不适合长期项目展示。
- Step 29 已准备 Release Notes 和 Evidence，但尚未形成 Git Tag、GitHub Release 与可下载固定资产。
- 仓库首页缺少简介和 Topics，不利于按 AI4SE、Agent、RAG、FastAPI、PostgreSQL 等维度快速识别项目。

为什么要添加：
- Git Tag 固定代码与文档版本，GitHub Release 提供稳定的展示和下载入口。
- Release Asset 保存关键证据摘要，避免 CI 原始 Artifact 到期后完全失去可追溯材料。
- SHA-256 可以验证下载资产没有被意外替换或损坏。
- Repository Description 和 Topics 能让项目定位在仓库列表和搜索结果中直接可见。

新增/操作内容：
- 在发布文档提交 `c7d27b3` 上创建并推送 annotated tag `v1.0.0`。
- 创建 GitHub Release `Software Engineering Agent v1.0.0`，Release ID `375795160`。
- 上传 `software-engineering-agent-v1.0.0-evidence.zip`，Asset ID `527781222`，大小 2489 bytes。
- Asset SHA-256：`F756FA99AD983A66D8EB8CB3AD1DF9DD7DF2DF151B60BB9C357139EABDFC4ABE`。
- Release Asset 包含 Release Notes、机器可读 Evidence 和 Public Release Audit；源码 zip/tar.gz 由 GitHub 根据 Tag 自动生成。
- 仓库简介更新为可复现 AI4SE Agent 定位，新增 `ai4se`、`llm-agent`、`rag`、`fastapi`、`postgresql`、`prometheus`、`agent-evaluation`、`python` Topics。

添加后的影响：
- 项目已有稳定的 v1.0.0 版本入口，代码、说明、CI 证据和下载资产形成闭环。
- 即使 Actions Artifact 到期，Release 中仍保留关键结果摘要与校验哈希。
- 仓库仍为 private，创建 Release 和 Topics 没有改变数据可见范围。

验证结果：
- Release URL：`https://github.com/Isoldelu/software-engineering-agent/releases/tag/v1.0.0`。
- Release 状态：draft=false、prerelease=false；Tag 已指向发布文档提交。
- Asset 状态：uploaded，远程大小与本地 2489 bytes 一致。
- Repository：private=true、visibility=private、default_branch=main。
- 本次发布未调用付费 Provider，Paid API Calls=0。

边界与诚实表述：
- Release 当前只对仓库授权用户可见；公开展示仍需仓库所有者显式切换 visibility。
- 固定证据包是结果摘要，不替代完整 CI 原始日志和生产容量报告。
- GitHub 自动源码归档包含 Tag 对应内容，后续 main 分支新增记录不会反向修改 v1.0.0。

下一步：
- Step 30 推荐先制作一张架构图、一个 2-3 分钟 API 演示和一份面试讲解提纲；真实 Provider A/B 可在 API Key 与小额预算明确后独立进行。

## Step 30：项目展示与面试交付

当前问题：
- 项目已有完整 Agent、评测和工程能力，但相关信息分散在 README、20 多份设计文档、API 页面和 CI Artifact 中，面试时难以在 2-3 分钟内形成清晰主线。
- 原 `interview_talking_points.md` 停留在较早阶段，仍写着真实 PostgreSQL CI 待执行，与 Step 28.1 后的实际状态不一致。
- FastAPI OpenAPI 版本仍为 `0.1.0`，与已发布的 `v1.0.0` 不一致。
- 原 Demo 详情面板展开完整 Evidence Path、Trace 和 Trajectory，会暴露本机绝对路径，并用大量 JSON 淹没工具链、证据和验证结果。
- 桌面页面的六个指标挤在一行，长 Intent 与答案会撑宽布局；移动端 Dashboard 的宽表把页面从 390 px 撑到 629 px。
- 缺少一条可重复执行的命令行演示路径，浏览器或现场网络异常时没有可靠降级方案。

为什么要添加：
- 展示层决定面试官能否快速识别“这不是普通 RAG”，需要用一张架构图串起 Planner、Tools、Evidence、Verifier、Trace、Feedback 和 Policy。
- 30 秒、2 分钟、5 分钟三档讲法可以根据面试节奏切换，避免只会背一段过长项目介绍。
- 可执行 Demo Script 能把口播内容绑定到真实 API 输出，现场可以直接看到工具序列、证据数量、Verifier 和 Trace 完整性。
- 桌面/移动端视觉验收可以提前发现绝对路径、横向溢出和信息层级问题，降低现场演示风险。
- 明确模拟数据、离线 Proxy、CI 功能压测和真实 Provider 未测边界，防止简历或面试表述越界。

新增/修改内容：
- 新增 `docs/project-showcase.md`：一页式定位、Mermaid 架构、Tool/RAG 分工、核心证据、工程决策和诚实边界。
- 新增 `docs/demo-runbook.md`：2-3 分钟时间轴、主 Query、备用 Query、现场故障降级和结束语。
- 新增 `examples/interview_demo.py`：使用标准库调用真实 FastAPI，支持 Base URL、Session、可选 API Key 和跳过实时评测。
- 命令行输出压缩为 Health、Multi-Tool Result、Replayable Trace 和可选 Frozen Evaluation 四段；Windows 输出显式使用 UTF-8。
- 重写 `docs/interview_talking_points.md`，增加 30 秒、2 分钟、5 分钟版本，覆盖 10 类高频追问和不可越过的合规边界。
- 更新 `docs/project_summary.md`、`docs/resume_version.md` 和 README，纠正 PostgreSQL CI 状态并增加 Interview-Ready Package。
- FastAPI 版本从 `0.1.0` 更新为 `1.0.0`，与 Git Tag/Release 对齐。
- 新增两张真实浏览器截图：`docs/assets/agent-demo.png` 与 `docs/assets/evaluation-dashboard.png`。
- Demo 第一屏改为 3×2 指标布局，补 `min-width: 0`、安全换行和稳定高度；标题改为 `Plan, Evidence And Trace`。
- Demo 详情不再显示原始 Evidence Path、完整 Trace 和 Trajectory，只展示稳定 `source_id`、Evidence/Citation、Verifier 和 Trace Summary。
- Evaluation Dashboard 第一屏 Highlights 从 12 条收敛为 6 条，让 Baseline 表进入首屏。
- Dashboard Section 增加局部 `overflow-x: auto`，宽表在区块内滚动，不再撑宽整个移动页面。
- 新增 `tests/test_step30_delivery.py`，保护展示文件、截图、Run ID、模拟数据边界、Demo 摘要、API 版本和隐私展示约束。
- `.gitignore` 增加 `.playwright-cli/`，避免浏览器会话、Console 和临时截图进入版本库。

实验问题与修复：
- 第一次命令行 Demo 在 Windows 终端出现中文乱码；通过 UTF-8 Stream Reconfigure 修复，并用 `getattr + callable` 保持 Mypy 兼容。
- Trace 摘要按不存在的 `replayable/events` 字段读取，出现 null/0；根据真实 `/traces/{id}` 契约改为 `trace_complete`、`steps` 和 `total_latency_ms`。
- 第一次桌面截图中，六指标单行导致 Intent 三行挤压，答案和 JSON 越出右侧；改为 3×2 指标并限制 Grid Item 最小宽度后，1440×1000 页面完整显示。
- 浏览器 Console 发现 `/favicon.ico` 404；两个页面加入内嵌空 Favicon 后 Console 无新增错误。
- 页面 Snapshot 发现完整 Trajectory 中包含 `<project-root>/data/...` 绝对路径；Demo 展示层改为 Stable Source ID 和 Trace Summary，API 原始契约不变。
- Playwright CLI 在后续 Node 24 Windows Daemon 上发生 TargetClosedError；改用同一 Playwright Core 直接启动 Chrome 完成移动端 DOM 尺寸检查，不把工具兼容问题误判为页面失败。
- 移动端 Demo 的 `body/document scrollWidth=390`，确认截图右边缘观感不是 DOM 溢出；Dashboard 首次为 629，定位到 Table 后修复为 390。

添加后的影响：
- GitHub 第一屏现在可直接进入项目展示、演示 Runbook、可执行 Demo、面试问答和 v1.0 Evidence。
- 面试展示从“翻多个文档解释功能”变为“一张架构图 + 一个复合 Query + 一个评测面板 + 一个绿色 CI Run”。
- Demo 重点集中在 Tools、Plan、Evidence、Citation、Verifier、Policy 和 Trace，不再暴露本机路径或展示无关内部结构。
- 浏览器不可用时可用一条命令完成相同主线；实时 Evaluation 较慢时可以使用 `--skip-evaluation` 和固定 CI Evidence 降级。
- 桌面和移动端都具有稳定布局，宽表在自身区块内滚动，不影响整个页面。
- 本步骤只调整展示、文档和 API Metadata，不修改 Router、Planner、Tool、Verifier、Policy 或冻结评测行为。

验证结果：
- 命令行主 Query `查一下 nginx 的版本变化和依赖`：3 个 Tool、3 条 Evidence、3 条 Citation、Verifier passed=true、Trace complete=true。
- 自动化测试：131 passed in 16.82s；Step 17 冻结评测仍为 193 条。
- Ruff：All checks passed。
- Mypy：42 个源码模块无类型问题。
- 桌面 Demo：Playwright 1440×1000 真实运行和截图通过，Console 无业务错误。
- 桌面 Dashboard：193 Cases、0 Bad Cases、Baseline 表和优化入口均在首屏可见。
- 移动端 Demo：viewport=390，body/document scrollWidth=390。
- 移动端 Dashboard：修复前 scrollWidth=629，修复后 body/document scrollWidth=390，193 条数据正常加载。
- Paid API Calls：0；真实 Provider A/B 未执行。
- GitHub Actions：本步骤提交前尚未运行，不能把本地 131 passed 记为远程 CI 结果。

边界与诚实表述：
- 两张截图来自本地模拟数据与 Offline Deterministic Provider，不代表真实线上数据或真实 LLM 输出。
- 193 条 100% 是项目冻结集结果，不代表开放领域泛化能力。
- Dashboard 的 Proxy Baseline 是离线可复现实验，不是 GPT、Claude 或 Gemini 的真实对比。
- GitHub PostgreSQL 100/40 请求是功能与恢复门禁，不是生产 SLA 或容量上限。
- 页面隐藏绝对路径是展示层隐私收敛，API 原始 Tool Observation 仍面向受信本地调试环境；公开部署还应增加响应级字段策略。

面试表达：
> 我没有在功能完成后就停止，而是把项目整理成了可面试交付的一套材料。主线是一张架构图和一个复合 Query：Planner 组合 Package、Dependency、Version 三个工具，结果经过 Evidence/Citation 和 Verifier，再生成可回放 Trace。评测面板展示 193 条冻结用例，以及 DirectLLMProxy、RAGOnlyProxy 和 Agent 的离线对比。我还用真实浏览器做了桌面和 390 px 移动端验收，发现并修复了绝对路径展示、六指标挤压和宽表把页面撑到 629 px 的问题。现场即使浏览器异常，也可以用标准库 Demo Script 输出相同工具链；全程使用模拟数据和离线 Provider，不冒充企业内部系统或真实模型 A/B。

下一步：
- 推送 Step 30 并等待 GitHub Actions 验证 131 项测试、Docker 与 PostgreSQL Job；成功后追加固定 Run 记录。
- Step 31 可二选一：优先制作 3-5 页面试演示 Deck/录屏，或在 API Key 与预算明确后执行小预算真实 Provider A/B。

## Step 30.1：GitHub Actions 远程交付验收

当前问题：
- Step 30 已完成本地 131 项测试和浏览器验收，但展示层修改包含 FastAPI HTML、截图、API Version 和新 Demo Script，仅有本机结果不足以作为最终交付证据。
- README 和面试材料仍指向 Step 29 的 128 项测试 Run，无法证明当前 Step 30 Commit 已通过 Docker 与真实 PostgreSQL 回归。

为什么要添加：
- 将本地展示验收与远程工程门禁绑定，证明文档交付没有破坏 Agent、Docker 或 PostgreSQL 多 Worker 路径。
- 使用固定 Commit、Run、Job 和 Artifact，面试展示可以直接跳到当前版本的可追溯证据。
- 保留 v1.0.0 历史 Evidence 不变，同时让 main 分支 README 指向最新验证结果。

执行与结果：
- Step 30 Commit：`5155f6200d2e8c98c8931c599c3ed86b504d0fc1`。
- GitHub Actions Run：`33309988987`，status=completed，conclusion=success。
- `test-and-evaluate` Job `99253176974`：success，131 tests passed。
- `docker-build` Job `99253177097`：success。
- `postgres-integration` Job `99253177070`：success。
- PostgreSQL Evidence Artifact：`software-agent-step28-postgres-evidence`，Artifact ID `9731680689`，expired=false，到期时间 2026-11-28。
- README、Project Showcase、Demo Runbook 和 Interview Talking Points 的当前 CI 入口更新为 Run `33309988987`。
- `v1.0.0` Tag、Release 和 `release/v1.0.0-evidence.json` 保持冻结，不因 main 分支展示更新而改写历史版本。

添加后的影响：
- Step 30 从本地展示材料升级为经过测试、镜像和真实 PostgreSQL 三重门禁的远程交付版本。
- 面试官可以从 README 截图进入项目，再直接打开同一版本的绿色 CI Run。
- 当前 main 分支验证数更新为 131；v1.0.0 的 128 项历史证据仍然可追溯，两者不混淆。

边界与诚实表述：
- 本次 Run 证明回归和部署门禁通过，不代表截图内容来自生产环境。
- PostgreSQL Artifact 仍有到期时间，长期证据继续以 Release 摘要和实验记录为准。
- 没有创建新的 Release 或移动 `v1.0.0` Tag；Step 30 是 main 分支展示交付增量。
- Paid API Calls=0，真实 Provider A/B 仍未执行。

下一步：
- Step 31 推荐优先制作 3-5 页项目演示 Deck：问题与定位、系统架构、一次 Query、评测优化、工程证据与边界。
- Deck 完成后再决定是否录制 2-3 分钟视频；真实 Provider A/B 继续等待 API Key 与预算。
